import type { Pattern } from "../types";

/** Two additional patterns for each of the three one-pattern topic buckets. */
export const PATTERNS_GAPS: readonly Pattern[] = [
  {
    t: "grid",
    n: "Flood fill & component labelling",
    tc: "O(mn)",
    sc: "O(mn) worst case",
    sig: "<b>Signal:</b> count islands, measure a connected region, capture surrounded cells, or replace every cell connected to a starting cell. Adjacency is normally four-directional unless the statement explicitly includes diagonals.",
    why: "Treat every cell as a graph vertex and generate its neighbours from four direction offsets. Starting one DFS or BFS from every unvisited land cell labels exactly one component; across all starts, each cell is entered at most once.",
    trap: "<b>Mark a cell visited when it is discovered, not when it is later removed from the stack or queue.</b> Otherwise several neighbours can add the same cell, multiplying the work. Also never assume diagonal adjacency unless the problem says so.",
    say: "The grid is an implicit graph; I start one traversal per unseen land cell and mark on discovery, so every cell and every constant-size neighbour list is processed once.",
    py: "def count_islands(grid):\n    if not grid or not grid[0]:\n        return 0\n    rows, cols = len(grid), len(grid[0])\n    islands = 0\n    for sr in range(rows):\n        for sc in range(cols):\n            if grid[sr][sc] != '1':\n                continue\n            islands += 1\n            grid[sr][sc] = '0'             # mark on discovery\n            stack = [(sr, sc)]\n            while stack:\n                r, c = stack.pop()\n                for dr, dc in ((1,0),(-1,0),(0,1),(0,-1)):\n                    nr, nc = r + dr, c + dc\n                    if 0 <= nr < rows and 0 <= nc < cols and grid[nr][nc] == '1':\n                        grid[nr][nc] = '0' # before pushing\n                        stack.append((nr, nc))\n    return islands",
    cpp: "int countIslands(vector<vector<char>>& g) {\n    if (g.empty() || g[0].empty()) return 0;\n    int R = g.size(), C = g[0].size(), ans = 0;\n    int dr[4] = {1,-1,0,0}, dc[4] = {0,0,1,-1};\n    for (int sr = 0; sr < R; ++sr) for (int sc = 0; sc < C; ++sc) {\n        if (g[sr][sc] != '1') continue;\n        ++ans; g[sr][sc] = '0';\n        vector<pair<int,int>> st{{sr, sc}};\n        while (!st.empty()) {\n            auto [r,c] = st.back(); st.pop_back();\n            for (int d = 0; d < 4; ++d) {\n                int nr=r+dr[d], nc=c+dc[d];\n                if (nr>=0 && nr<R && nc>=0 && nc<C && g[nr][nc]=='1') {\n                    g[nr][nc]='0'; st.push_back({nr,nc});\n                }\n            }\n        }\n    }\n    return ans;\n}",
    java: "int countIslands(char[][] g) {\n    if (g.length == 0 || g[0].length == 0) return 0;\n    int R=g.length, C=g[0].length, ans=0;\n    int[][] dirs={{1,0},{-1,0},{0,1},{0,-1}};\n    for (int sr=0; sr<R; sr++) for (int sc=0; sc<C; sc++) {\n        if (g[sr][sc] != '1') continue;\n        ans++; g[sr][sc]='0';\n        Deque<int[]> st=new ArrayDeque<>(); st.push(new int[]{sr,sc});\n        while (!st.isEmpty()) {\n            int[] cur=st.pop();\n            for (int[] d:dirs) {\n                int nr=cur[0]+d[0], nc=cur[1]+d[1];\n                if (nr>=0 && nr<R && nc>=0 && nc<C && g[nr][nc]=='1') {\n                    g[nr][nc]='0'; st.push(new int[]{nr,nc});\n                }\n            }\n        }\n    }\n    return ans;\n}",
    lc: [
      [200, "number-of-islands", "Number of Islands"],
      [695, "max-area-of-island", "Max Area of Island"],
      [130, "surrounded-regions", "Surrounded Regions"],
      [733, "flood-fill", "Flood Fill"],
    ],
    edges: [
      { input: "Empty grid or an empty first row", effect: "Reading the first cell or its width throws before traversal begins.", fix: "Guard both shapes before computing rows and columns." },
      { input: "A 1000×1000 solid island", effect: "Recursive DFS can overflow Python or JVM stacks even though the algorithm is linear.", fix: "Use an explicit stack or queue for unbounded component size." },
      { input: "Visited marked on pop", effect: "The same open cell may be pushed by several neighbours, inflating time and memory.", fix: "Mark at the same moment the cell is pushed." },
      { input: "Diagonal cells touch at a corner", effect: "They are incorrectly merged if eight directions are generated for a four-direction problem.", fix: "Encode exactly the adjacency definition given by the statement." },
    ],
    walk: [{
      title: "Two islands, one traversal each",
      input: "[[1,1,0],[0,1,0],[1,0,1]]",
      cols: ["scan reaches", "component", "cells consumed", "count"],
      rows: [
        ["(0,0)", "A", "(0,0),(0,1),(1,1)", "1"],
        ["(2,0)", "B", "(2,0)", "2"],
        ["(2,2)", "C", "(2,2)", "<b>3</b>"],
      ],
      lesson: "Each traversal consumes its whole component by changing land to water. Later scan positions therefore skip cells already owned by an earlier component, which is the reason the total work remains linear.",
    }],
  },
  {
    t: "grid",
    n: "Multi-source BFS on a grid",
    tc: "O(mn)",
    sc: "O(mn)",
    sig: "<b>Signal:</b> nearest zero, distance to the closest gate, infection spreading each minute, or simultaneous expansion from several starting cells in an unweighted grid.",
    why: "Put every source in the queue at distance zero before BFS begins. The queue then processes the union of their distance layers, so the first time a cell is reached is its minimum distance to any source — one BFS replaces running a separate search from every cell.",
    trap: "Running BFS once per destination is O((mn)²). The direction of the search is the trick: reverse it and expand from all sources together. Mark on enqueue so competing waves cannot duplicate a cell.",
    say: "I reverse the search, seed every source at distance zero, and run one BFS; the first wave to reach a cell is its nearest source because all edges have unit cost.",
    py: "from collections import deque\n\ndef nearest_zero(mat):\n    rows, cols = len(mat), len(mat[0])\n    dist = [[-1] * cols for _ in range(rows)]\n    q = deque()\n    for r in range(rows):\n        for c in range(cols):\n            if mat[r][c] == 0:\n                dist[r][c] = 0\n                q.append((r, c))           # every source at layer 0\n    while q:\n        r, c = q.popleft()\n        for dr, dc in ((1,0),(-1,0),(0,1),(0,-1)):\n            nr, nc = r + dr, c + dc\n            if 0 <= nr < rows and 0 <= nc < cols and dist[nr][nc] == -1:\n                dist[nr][nc] = dist[r][c] + 1\n                q.append((nr, nc))\n    return dist",
    cpp: "vector<vector<int>> nearestZero(vector<vector<int>>& a) {\n    int R=a.size(), C=a[0].size();\n    vector<vector<int>> d(R, vector<int>(C, -1));\n    queue<pair<int,int>> q;\n    for (int r=0;r<R;r++) for(int c=0;c<C;c++) if(a[r][c]==0) { d[r][c]=0; q.push({r,c}); }\n    int dr[4]={1,-1,0,0}, dc[4]={0,0,1,-1};\n    while(!q.empty()) {\n        auto [r,c]=q.front(); q.pop();\n        for(int k=0;k<4;k++) {\n            int nr=r+dr[k], nc=c+dc[k];\n            if(nr>=0&&nr<R&&nc>=0&&nc<C&&d[nr][nc]==-1) {\n                d[nr][nc]=d[r][c]+1; q.push({nr,nc});\n            }\n        }\n    }\n    return d;\n}",
    java: "int[][] nearestZero(int[][] a) {\n    int R=a.length, C=a[0].length;\n    int[][] d=new int[R][C];\n    for(int[] row:d) Arrays.fill(row,-1);\n    Deque<int[]> q=new ArrayDeque<>();\n    for(int r=0;r<R;r++) for(int c=0;c<C;c++) if(a[r][c]==0) { d[r][c]=0; q.add(new int[]{r,c}); }\n    int[][] dirs={{1,0},{-1,0},{0,1},{0,-1}};\n    while(!q.isEmpty()) {\n        int[] cur=q.remove();\n        for(int[] x:dirs) {\n            int nr=cur[0]+x[0], nc=cur[1]+x[1];\n            if(nr>=0&&nr<R&&nc>=0&&nc<C&&d[nr][nc]==-1) {\n                d[nr][nc]=d[cur[0]][cur[1]]+1; q.add(new int[]{nr,nc});\n            }\n        }\n    }\n    return d;\n}",
    lc: [
      [542, "01-matrix", "01 Matrix"],
      [994, "rotting-oranges", "Rotting Oranges"],
      [1162, "as-far-from-land-as-possible", "As Far from Land as Possible"],
      [286, "walls-and-gates", "Walls and Gates"],
    ],
    edges: [
      { input: "No source cells", effect: "The queue starts empty and unreachable cells retain their sentinel distance.", fix: "Return the sentinel or the problem's required impossible value explicitly." },
      { input: "All cells are sources", effect: "Every distance is zero and no neighbour is added.", fix: "This is correct; keep the layer-zero initialisation separate from expansion." },
      { input: "Weighted moves", effect: "FIFO order no longer guarantees the first arrival is cheapest.", fix: "Use 0-1 BFS for weights 0/1 or Dijkstra for general non-negative weights." },
      { input: "Source added after BFS begins", effect: "Its zero-distance wave is processed too late and earlier cells keep non-minimal answers.", fix: "Collect every source before the first dequeue." },
    ],
    walk: [{
      title: "Two waves meet",
      input: "[[0,1,1],[1,1,1],[1,1,0]]",
      cols: ["layer", "frontier", "new distances"],
      rows: [
        ["0", "(0,0), (2,2)", "sources = 0"],
        ["1", "(1,0),(0,1),(1,2),(2,1)", "four cells = 1"],
        ["2", "(1,1),(0,2),(2,0)", "remaining cells = <b>2</b>"],
      ],
      lesson: "Both sources enter before layer one begins, so their waves compete fairly. A cell is claimed by whichever layer reaches it first; no per-cell search and no later relaxation are required for unit edges.",
    }],
  },
  {
    t: "trie",
    n: "Trie-pruned board search",
    tc: "O(mn·4^L) worst case",
    sc: "O(dictionary characters + L)",
    sig: "<b>Signal:</b> find many dictionary words in one letter board. A separate DFS per word repeats the same prefixes; a trie lets all words share one traversal and kills a branch as soon as it stops matching every dictionary prefix.",
    why: "The DFS path and trie path advance together. If a board prefix has no trie child, no dictionary word can extend it, so the entire subtree is safely pruned. Storing the completed word at terminal nodes also avoids rebuilding strings and deduplicates results.",
    trap: "You must restore the board cell after the recursive calls. Forgetting restoration makes later starts behave as if earlier paths permanently consumed cells. Also remove or clear a found terminal word so the same word is not emitted many times.",
    say: "The trie merges all dictionary-prefix searches; DFS stops at the first absent trie edge, and I restore the board cell on unwind so paths remain independent.",
    py: "def find_words(board, words):\n    root = {}\n    for word in words:\n        node = root\n        for ch in word:\n            node = node.setdefault(ch, {})\n        node['$'] = word\n    rows, cols, out = len(board), len(board[0]), []\n\n    def dfs(r, c, node):\n        ch = board[r][c]\n        if ch not in node:\n            return\n        nxt = node[ch]\n        word = nxt.pop('$', None)       # emit once\n        if word is not None:\n            out.append(word)\n        board[r][c] = '#'\n        for dr, dc in ((1,0),(-1,0),(0,1),(0,-1)):\n            nr, nc = r + dr, c + dc\n            if 0 <= nr < rows and 0 <= nc < cols and board[nr][nc] != '#':\n                dfs(nr, nc, nxt)\n        board[r][c] = ch                # restore on unwind\n        if not nxt:\n            node.pop(ch)                # optional structural pruning\n\n    for r in range(rows):\n        for c in range(cols):\n            dfs(r, c, root)\n    return out",
    cpp: "struct Node { unordered_map<char,Node*> kid; string word; };\nvector<string> findWords(vector<vector<char>>& b, vector<string>& words) {\n    Node* root=new Node();\n    for(auto& w:words){ Node* n=root; for(char ch:w) { if(!n->kid[ch]) n->kid[ch]=new Node(); n=n->kid[ch]; } n->word=w; }\n    int R=b.size(), C=b[0].size(); vector<string> out;\n    function<void(int,int,Node*)> dfs = [&](int r,int c,Node* n){\n        char ch=b[r][c]; auto it=n->kid.find(ch); if(it==n->kid.end()) return;\n        Node* x=it->second; if(!x->word.empty()){ out.push_back(x->word); x->word.clear(); }\n        b[r][c]='#'; int dr[4]={1,-1,0,0},dc[4]={0,0,1,-1};\n        for(int k=0;k<4;k++){ int nr=r+dr[k],nc=c+dc[k]; if(nr>=0&&nr<R&&nc>=0&&nc<C&&b[nr][nc]!='#') dfs(nr,nc,x); }\n        b[r][c]=ch;\n    };\n    for(int r=0;r<R;r++) for(int c=0;c<C;c++) dfs(r,c,root);\n    return out;\n}",
    java: "class Node { Map<Character,Node> kid=new HashMap<>(); String word; }\nList<String> findWords(char[][] b, String[] words) {\n    Node root=new Node();\n    for(String w:words){ Node n=root; for(char ch:w.toCharArray()) n=n.kid.computeIfAbsent(ch,k->new Node()); n.word=w; }\n    List<String> out=new ArrayList<>();\n    for(int r=0;r<b.length;r++) for(int c=0;c<b[0].length;c++) dfs(b,r,c,root,out);\n    return out;\n}\nvoid dfs(char[][] b,int r,int c,Node n,List<String> out){\n    char ch=b[r][c]; Node x=n.kid.get(ch); if(x==null) return;\n    if(x.word!=null){ out.add(x.word); x.word=null; }\n    b[r][c]='#'; int[][] ds={{1,0},{-1,0},{0,1},{0,-1}};\n    for(int[] d:ds){ int nr=r+d[0],nc=c+d[1]; if(nr>=0&&nr<b.length&&nc>=0&&nc<b[0].length&&b[nr][nc]!='#') dfs(b,nr,nc,x,out); }\n    b[r][c]=ch;\n}",
    lc: [
      [212, "word-search-ii", "Word Search II"],
      [79, "word-search", "Word Search"],
      [720, "longest-word-in-dictionary", "Longest Word in Dictionary"],
      [425, "word-squares", "Word Squares"],
    ],
    edges: [
      { input: "The same word has several board paths", effect: "It is appended repeatedly unless its terminal marker is cleared.", fix: "Store the word at the terminal node and clear it immediately after the first emission." },
      { input: "A cell is not restored after DFS", effect: "Later starting positions see a permanent visited marker and miss valid words.", fix: "Save the character, mark it, recurse, then restore it on every normal return path." },
      { input: "Empty board or dictionary", effect: "Reading the first row or constructing needless trie nodes fails or wastes work.", fix: "Return an empty result before building or scanning." },
      { input: "A word is a prefix of a longer word", effect: "Stopping at the first terminal loses the longer word.", fix: "Emit the terminal word but continue exploring its children." },
    ],
    walk: [{
      title: "Pruning a dead prefix",
      input: "board path o→a→x; dictionary = [oath, oat, eat]",
      cols: ["board prefix", "trie node?", "action"],
      rows: [
        ["o", "yes", "continue"],
        ["oa", "yes", "continue"],
        ["oat", "terminal", "emit <b>oat</b>; continue for oath"],
        ["oax", "no", "return immediately"],
      ],
      lesson: "The absent x edge proves that no dictionary word begins with oax, so every longer board path under that state is discarded in one check. That shared prefix pruning is the advantage over one DFS per word.",
    }],
  },
  {
    t: "trie",
    n: "Binary trie for maximum XOR",
    tc: "O(n·B)",
    sc: "O(n·B)",
    sig: "<b>Signal:</b> maximise XOR between pairs, answer maximum-XOR queries, or count pairs by XOR value. The fixed-width binary representation is a trie whose alphabet has only 0 and 1.",
    why: "At each bit from most significant to least, XOR gains that bit exactly when the partner uses the opposite bit. Greedily take the opposite child when it exists; higher bits dominate every combination of lower bits, so the local choice is globally optimal.",
    trap: "Choose and state the bit width. Starting below the highest possible bit silently truncates values; using signed right shift on negative Java integers can also propagate ones. Most OA versions use non-negative 31-bit inputs.",
    say: "I walk from the most significant bit and prefer the opposite child; setting a higher XOR bit is worth more than every lower bit combined, which proves the greedy choice.",
    py: "def maximum_xor(nums):\n    root = {}\n    def insert(x):\n        node = root\n        for bit in range(30, -1, -1):\n            b = (x >> bit) & 1\n            node = node.setdefault(b, {})\n    def best(x):\n        node = root\n        value = 0\n        for bit in range(30, -1, -1):\n            b = (x >> bit) & 1\n            want = b ^ 1\n            if want in node:\n                value |= 1 << bit\n                node = node[want]\n            else:\n                node = node[b]\n        return value\n    for x in nums:\n        insert(x)\n    return max((best(x) for x in nums), default=0)",
    cpp: "struct Node { int next[2] = {-1,-1}; };\nint maximumXor(vector<int>& a) {\n    vector<Node> tr(1);\n    for(int x:a){ int n=0; for(int bit=30;bit>=0;--bit){ int b=(x>>bit)&1; if(tr[n].next[b]<0){tr[n].next[b]=tr.size();tr.push_back(Node());} n=tr[n].next[b]; } }\n    int ans=0;\n    for(int x:a){ int n=0,cur=0; for(int bit=30;bit>=0;--bit){ int b=(x>>bit)&1,w=b^1; if(tr[n].next[w]>=0){cur|=1<<bit;n=tr[n].next[w];}else n=tr[n].next[b]; } ans=max(ans,cur); }\n    return ans;\n}",
    java: "static class Node { Node[] next=new Node[2]; }\nint maximumXor(int[] a) {\n    Node root=new Node();\n    for(int x:a){ Node n=root; for(int bit=30;bit>=0;bit--){int b=(x>>>bit)&1;if(n.next[b]==null)n.next[b]=new Node();n=n.next[b];}}\n    int ans=0;\n    for(int x:a){Node n=root;int cur=0;for(int bit=30;bit>=0;bit--){int b=(x>>>bit)&1,w=b^1;if(n.next[w]!=null){cur|=1<<bit;n=n.next[w];}else n=n.next[b];}ans=Math.max(ans,cur);}\n    return ans;\n}",
    lc: [
      [421, "maximum-xor-of-two-numbers-in-an-array", "Maximum XOR of Two Numbers"],
      [1707, "maximum-xor-with-an-element-from-array", "Maximum XOR With an Element"],
      [1803, "count-pairs-with-xor-in-a-range", "Count Pairs With XOR in a Range"],
    ],
    edges: [
      { input: "Empty input", effect: "There is no trie path to follow and a max over no candidates fails.", fix: "Return 0 or the problem's sentinel before querying." },
      { input: "Values may use bit 31", effect: "A loop from bit 30 silently ignores the sign/high bit and orders XOR choices incorrectly.", fix: "Derive B from the constraint and use unsigned shifts where the language requires them." },
      { input: "Query before any insertion", effect: "Both children are absent and traversal dereferences null.", fix: "Insert the allowed candidate set before querying, or maintain an explicit trie size." },
      { input: "Constrained queries x, m", effect: "Using every number can select a partner greater than m.", fix: "Sort numbers and queries by m, inserting only values that have become eligible." },
    ],
    walk: [{
      title: "Higher bits decide first",
      input: "query 5 (0101), candidates 2 (0010), 8 (1000)",
      cols: ["partner", "xor", "binary", "winner"],
      rows: [
        ["2", "7", "0111", ""],
        ["8", "13", "1101", "<b>yes</b>"],
        ["MSB choice", "opposite 1", "sets bit 3", "worth 8 by itself"],
      ],
      lesson: "Choosing candidate 8 sets the top XOR bit. Even if candidate 2 set every remaining lower bit, their total value would be only 7, so the opposite-child decision at each level is safe.",
    }],
  },
  {
    t: "dsu",
    n: "Kruskal minimum spanning tree",
    tc: "O(E log E)",
    sc: "O(V)",
    sig: "<b>Signal:</b> connect every vertex with minimum total edge cost, edges are given explicitly, and cycles are forbidden. Sorting edges plus Union-Find is Kruskal's algorithm.",
    why: "Process edges from lightest to heaviest and take an edge only when it joins two different components. By the cut property, the lightest edge crossing any component cut is safe; Union-Find answers whether the endpoints are already connected.",
    trap: "Stop after exactly V−1 accepted edges, not after examining V−1 edges. If fewer than V−1 can be accepted, the graph is disconnected and has no spanning tree. Use a wide integer for the total cost.",
    say: "I sort by weight and accept an edge only when Union-Find says it joins two components; the cut property makes each accepted lightest crossing edge safe.",
    py: "def kruskal(n, edges):\n    parent = list(range(n))\n    size = [1] * n\n    def find(x):\n        while parent[x] != x:\n            parent[x] = parent[parent[x]]\n            x = parent[x]\n        return x\n    total = used = 0\n    for w, a, b in sorted(edges):\n        ra, rb = find(a), find(b)\n        if ra == rb:\n            continue\n        if size[ra] < size[rb]:\n            ra, rb = rb, ra\n        parent[rb] = ra\n        size[ra] += size[rb]\n        total += w\n        used += 1\n        if used == n - 1:\n            return total\n    return -1 if n > 1 else 0",
    cpp: "long long kruskal(int n, vector<array<int,3>>& e) {\n    sort(e.begin(),e.end()); vector<int> p(n),sz(n,1); iota(p.begin(),p.end(),0);\n    function<int(int)> find=[&](int x){while(p[x]!=x){p[x]=p[p[x]];x=p[x];}return x;};\n    long long total=0; int used=0;\n    for(auto [w,a,b]:e){int ra=find(a),rb=find(b);if(ra==rb)continue;if(sz[ra]<sz[rb])swap(ra,rb);p[rb]=ra;sz[ra]+=sz[rb];total+=w;if(++used==n-1)return total;}\n    return n<=1?0:-1;\n}",
    java: "long kruskal(int n, int[][] edges) {\n    Arrays.sort(edges,Comparator.comparingInt(e->e[0])); int[] p=new int[n],sz=new int[n];\n    for(int i=0;i<n;i++){p[i]=i;sz[i]=1;} long total=0; int used=0;\n    for(int[] e:edges){int a=e[1],b=e[2];while(p[a]!=a){p[a]=p[p[a]];a=p[a];}while(p[b]!=b){p[b]=p[p[b]];b=p[b];}if(a==b)continue;if(sz[a]<sz[b]){int t=a;a=b;b=t;}p[b]=a;sz[a]+=sz[b];total+=e[0];if(++used==n-1)return total;}\n    return n<=1?0:-1;\n}",
    lc: [
      [1584, "min-cost-to-connect-all-points", "Min Cost to Connect All Points"],
      [1135, "connecting-cities-with-minimum-cost", "Connecting Cities With Minimum Cost"],
      [1168, "optimize-water-distribution-in-a-village", "Optimize Water Distribution"],
    ],
    edges: [
      { input: "Disconnected graph", effect: "The loop ends with fewer than V−1 accepted edges, so the accumulated forest is not a spanning tree.", fix: "Track accepted edges and return impossible unless the count reaches V−1." },
      { input: "Parallel edges", effect: "Both are legal input, but taking the expensive one first can destroy optimality.", fix: "Global weight sort handles them naturally; the cheaper edge is considered first." },
      { input: "Negative edge weights", effect: "They may look suspicious but do not invalidate minimum spanning trees.", fix: "Keep them; ascending sort correctly accepts safe negative edges first." },
      { input: "Large weights and many vertices", effect: "The MST total can overflow a 32-bit integer while every individual weight fits.", fix: "Accumulate into Python int, C++ long long, or Java long." },
    ],
    walk: [{
      title: "Rejecting the first cycle",
      input: "edges (1,A,B), (2,B,C), (3,A,C), (7,C,D)",
      cols: ["edge", "roots before", "action", "total"],
      rows: [
        ["1 A-B", "A, B", "take", "1"],
        ["2 B-C", "A, C", "take", "3"],
        ["3 A-C", "A, A", "<b>skip cycle</b>", "3"],
        ["7 C-D", "A, D", "take; V−1 reached", "<b>10</b>"],
      ],
      lesson: "The third edge is cheap but adds no new connectivity, so accepting it would spend cost on a cycle. Union-Find turns that proof obligation into one root comparison.",
    }],
  },
  {
    t: "dsu",
    n: "Offline connectivity with sorted queries",
    tc: "O((E+Q) log(E+Q))",
    sc: "O(V+Q)",
    sig: "<b>Signal:</b> many connectivity queries each with a threshold such as 'is there a path using only edges below limit?'. Queries do not mutate the graph and may be answered in any order.",
    why: "Sort edges by weight and queries by threshold. Sweep thresholds upward, unioning every edge that has become legal before answering a query. The DSU at that instant represents exactly the subgraph allowed by that query; original query indices restore output order.",
    trap: "Match the inequality exactly. If the query says edge weight strictly less than limit, union while weight &lt; limit, not ≤. Equal-weight boundary cases are the hidden test.",
    say: "Because queries are offline, I sort them by limit and incrementally union eligible edges; at each query the DSU is exactly the graph below that threshold.",
    py: "def distance_limited_paths(n, edges, queries):\n    parent = list(range(n))\n    size = [1] * n\n    def find(x):\n        while parent[x] != x:\n            parent[x] = parent[parent[x]]\n            x = parent[x]\n        return x\n    def union(a, b):\n        a, b = find(a), find(b)\n        if a == b:\n            return\n        if size[a] < size[b]:\n            a, b = b, a\n        parent[b] = a\n        size[a] += size[b]\n    edges.sort(key=lambda e: e[2])\n    ordered = sorted((limit, a, b, i) for i, (a, b, limit) in enumerate(queries))\n    ans = [False] * len(queries)\n    j = 0\n    for limit, a, b, i in ordered:\n        while j < len(edges) and edges[j][2] < limit:\n            union(edges[j][0], edges[j][1])\n            j += 1\n        ans[i] = find(a) == find(b)\n    return ans",
    cpp: "vector<bool> limited(int n, vector<vector<int>>& e, vector<vector<int>>& q) {\n    sort(e.begin(),e.end(),[](auto&a,auto&b){return a[2]<b[2];}); vector<array<int,4>> qs;\n    for(int i=0;i<(int)q.size();i++)qs.push_back({q[i][2],q[i][0],q[i][1],i}); sort(qs.begin(),qs.end());\n    vector<int> p(n),sz(n,1);iota(p.begin(),p.end(),0);auto find=[&](int x){while(p[x]!=x){p[x]=p[p[x]];x=p[x];}return x;};\n    vector<bool> ans(q.size());int j=0;for(auto [lim,a,b,i]:qs){while(j<(int)e.size()&&e[j][2]<lim){int x=find(e[j][0]),y=find(e[j][1]);if(x!=y){if(sz[x]<sz[y])swap(x,y);p[y]=x;sz[x]+=sz[y];}j++;}ans[i]=find(a)==find(b);}return ans;\n}",
    java: "boolean[] limited(int n,int[][] e,int[][] q){\n    Arrays.sort(e,Comparator.comparingInt(x->x[2]));Integer[] order=new Integer[q.length];for(int i=0;i<q.length;i++)order[i]=i;Arrays.sort(order,Comparator.comparingInt(i->q[i][2]));\n    int[] p=new int[n],sz=new int[n];for(int i=0;i<n;i++){p[i]=i;sz[i]=1;}boolean[] ans=new boolean[q.length];int j=0;\n    for(int qi:order){while(j<e.length&&e[j][2]<q[qi][2]){int a=e[j][0],b=e[j][1];while(p[a]!=a){p[a]=p[p[a]];a=p[a];}while(p[b]!=b){p[b]=p[p[b]];b=p[b];}if(a!=b){if(sz[a]<sz[b]){int t=a;a=b;b=t;}p[b]=a;sz[a]+=sz[b];}j++;}int a=q[qi][0],b=q[qi][1];while(p[a]!=a)a=p[a];while(p[b]!=b)b=p[b];ans[qi]=a==b;}return ans;\n}",
    lc: [
      [1697, "checking-existence-of-edge-length-limited-paths", "Distance Limited Paths"],
      [1724, "checking-existence-of-edge-length-limited-paths-ii", "Distance Limited Paths II"],
      [2421, "number-of-good-paths", "Number of Good Paths"],
    ],
    edges: [
      { input: "An edge weight equals the query limit", effect: "It is incorrectly admitted if the statement requires strictly smaller edges.", fix: "Write the sweep comparison directly from the statement and test equality." },
      { input: "Queries returned in sorted order", effect: "Answers no longer line up with the caller's original query sequence.", fix: "Carry each original index through the sorted sweep." },
      { input: "Queries processed online", effect: "Rebuilding connectivity from scratch per threshold costs O(QE).", fix: "Exploit that answers are independent and sort the whole batch offline." },
      { input: "A lower threshold appears after a higher one", effect: "DSU cannot remove edges, so input order cannot be used directly.", fix: "Sorting thresholds monotonically upward is what makes one-way unions valid." },
    ],
    walk: [{
      title: "One monotone edge sweep",
      input: "edges: 0-1@2, 1-2@5; queries: (0,2,&lt;6), (0,2,&lt;5)",
      cols: ["query after sort", "edges admitted", "connected?", "saved at index"],
      rows: [
        ["limit 5", "0-1@2", "no", "1"],
        ["limit 6", "plus 1-2@5", "<b>yes</b>", "0"],
        ["return", "—", "—", "[true, false]"],
      ],
      lesson: "The equal-weight edge at limit 5 is excluded because the inequality is strict, then becomes eligible at limit 6. Original indices recover the requested answer order after the threshold sort.",
    }],
  },
] as const;
