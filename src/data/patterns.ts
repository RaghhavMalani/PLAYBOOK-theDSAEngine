import type { Pattern, Topic } from "../types";

export const TOPICS: readonly Topic[] = [
  ["hash", "Hashing"],
  ["str", "Strings"],
  ["arr", "Arrays & two pointers"],
  ["bs", "Binary search"],
  ["ll", "Linked lists"],
  ["stk", "Stacks & queues"],
  ["tree", "Trees & BST"],
  ["heap", "Heaps"],
  ["graph", "Graphs"],
  ["dp", "Dynamic programming"],
  ["bt", "Backtracking"],
  ["greedy", "Greedy"],
  ["bit", "Bits & math"],
] as const;

export const PATTERNS: readonly Pattern[] = [
  {
    t: "hash",
    n: "Frequency map",
    tc: "O(n)",
    sc: "O(k)",
    sig: "<b>Signal:</b> counting occurrences, finding a majority, checking if two collections contain the same multiset, or anything phrased as <em>how many times</em>.",
    why: "A hash map turns 'how many of x' from an O(n) scan into an O(1) lookup. Almost every counting problem is one pass to build the map and one pass to read it.",
    trap: "In C++, <code>m[key]</code> on a missing key <b>inserts</b> a default-constructed entry. Inside a read-only loop that silently grows the map and can flip a <code>.size()</code> check. Use <code>m.count(k)</code> or <code>m.find(k)</code> to test membership.",
    say: "One pass to build, one to query, so it's O(n) time and O(k) space where k is the number of distinct keys.",
    py: "from collections import Counter\ncnt = Counter(a)              # dict: value -> count\n\n# most common\nval, freq = cnt.most_common(1)[0]\n\n# manual, no import\nd = {}\nfor v in a:\n    d[v] = d.get(v, 0) + 1",
    cpp: "unordered_map<int,int> cnt;\nfor (int v : a) cnt[v]++;      // [] default-constructs to 0 here: fine\n\n// membership WITHOUT inserting\nif (cnt.count(x)) { /* ... */ }\n\nint best = 0, bv = 0;\nfor (auto& [v, c] : cnt)\n    if (c > best) { best = c; bv = v; }",
    java: "Map<Integer,Integer> cnt = new HashMap<>();\nfor (int v : a) cnt.merge(v, 1, Integer::sum);\n\n// or\nfor (int v : a) cnt.put(v, cnt.getOrDefault(v, 0) + 1);\n\nint bv = Collections.max(cnt.entrySet(),\n            Map.Entry.comparingByValue()).getKey();",
    lc: [
      [242, "valid-anagram", "Valid Anagram"],
      [169, "majority-element", "Majority Element"],
      [347, "top-k-frequent-elements", "Top K Frequent"],
      [383, "ransom-note", "Ransom Note"],
    ],
  },
  {
    t: "hash",
    n: "Complement lookup",
    tc: "O(n)",
    sc: "O(n)",
    sig: "<b>Signal:</b> find two elements with a given sum/difference, and you need the original indices or cannot sort.",
    why: "You are not searching for a value, you are searching for its complement. Check before you insert and you can never pair an element with itself.",
    trap: "Insert <em>after</em> the check, never before — otherwise <code>target = 2*v</code> matches a[i] with itself. In C++, <code>unordered_map</code> degrades to O(n) per op under adversarial hashing; competitive judges exploit this, so reserve and consider a custom hash on hot paths.",
    say: "This trades O(n) space for the log factor sorting would cost, and it preserves the original indices.",
    py: "seen = {}\nfor i, v in enumerate(a):\n    if target - v in seen:\n        return seen[target - v], i\n    seen[v] = i          # AFTER the check\nreturn None",
    cpp: "unordered_map<int,int> seen;\nseen.reserve(a.size() * 2);        // fewer rehashes\nfor (int i = 0; i < (int)a.size(); i++) {\n    auto it = seen.find(target - a[i]);\n    if (it != seen.end()) return {it->second, i};\n    seen[a[i]] = i;\n}\nreturn {-1, -1};",
    java: "Map<Integer,Integer> seen = new HashMap<>();\nfor (int i = 0; i < a.length; i++) {\n    Integer j = seen.get(target - a[i]);\n    if (j != null) return new int[]{j, i};\n    seen.put(a[i], i);\n}\nreturn new int[]{-1, -1};",
    lc: [
      [1, "two-sum", "Two Sum"],
      [454, "4sum-ii", "4Sum II"],
      [219, "contains-duplicate-ii", "Contains Duplicate II"],
      [1, "two-sum", "Two Sum"],
    ],
  },
  {
    t: "hash",
    n: "Prefix sum + hash map",
    tc: "O(n)",
    sc: "O(n)",
    sig: "<b>Signal:</b> <em>count</em> or <em>find</em> subarrays whose sum equals k, or is divisible by k. Negative numbers are present, so sliding window is out.",
    why: "sum(l..r) == k rewrites as prefix[r] - prefix[l-1] == k. Sweep once holding a running prefix and ask the map how many earlier prefixes equal running - k. That is two-sum on prefixes.",
    trap: "Seed the map with <code>{0: 1}</code> or you miss every subarray starting at index 0. For the divisible-by-k variant, key on <code>running % k</code> and normalise negatives with <code>((r % k) + k) % k</code>.",
    say: "Sliding window needs monotonicity; with negatives that breaks, so prefix sums plus a map is the correct tool.",
    py: "from collections import defaultdict\nseen = defaultdict(int)\nseen[0] = 1                 # the empty prefix\nrun = out = 0\nfor v in a:\n    run += v\n    out += seen[run - k]    # how many valid left ends\n    seen[run] += 1\nreturn out",
    cpp: "unordered_map<long long,int> seen;\nseen[0] = 1;\nlong long run = 0; int out = 0;\nfor (int v : a) {\n    run += v;\n    auto it = seen.find(run - k);\n    if (it != seen.end()) out += it->second;\n    seen[run]++;\n}\nreturn out;",
    java: "Map<Long,Integer> seen = new HashMap<>();\nseen.put(0L, 1);\nlong run = 0; int out = 0;\nfor (int v : a) {\n    run += v;\n    out += seen.getOrDefault(run - k, 0);\n    seen.merge(run, 1, Integer::sum);\n}\nreturn out;",
    lc: [
      [560, "subarray-sum-equals-k", "Subarray Sum Equals K"],
      [974, "subarray-sums-divisible-by-k", "Sums Divisible by K"],
      [523, "continuous-subarray-sum", "Continuous Subarray Sum"],
      [325, "maximum-size-subarray-sum-equals-k", "Max Size Subarray Sum k"],
    ],
  },
  {
    t: "hash",
    n: "Group by canonical key",
    tc: "O(n·k)",
    sc: "O(n·k)",
    sig: "<b>Signal:</b> group items that are 'the same' under some transformation — anagrams, shifted strings, equivalent shapes.",
    why: "Define a canonical form that is identical for every member of a group, then use it as a map key. The whole problem collapses to choosing the right key.",
    trap: "Sorting each string is O(k log k); a 26-length count tuple is O(k) and faster when strings are long. In Java the key must be a value type — <code>int[]</code> hashes by identity, so build a String key instead.",
    say: "The algorithm is trivial; the design decision is the canonical key, and that is what I'd optimise.",
    py: "from collections import defaultdict\ngroups = defaultdict(list)\nfor s in words:\n    key = tuple(sorted(s))          # or a 26-length count tuple\n    groups[key].append(s)\nreturn list(groups.values())",
    cpp: "unordered_map<string, vector<string>> groups;\nfor (auto& s : words) {\n    string key(26, '0');\n    for (char c : s) key[c - 'a']++;\n    groups[key].push_back(s);\n}\nvector<vector<string>> out;\nfor (auto& [k, v] : groups) out.push_back(v);\nreturn out;",
    java: "Map<String,List<String>> groups = new HashMap<>();\nfor (String s : words) {\n    char[] c = s.toCharArray();\n    Arrays.sort(c);\n    String key = new String(c);   // int[] would hash by identity\n    groups.computeIfAbsent(key, x -> new ArrayList<>()).add(s);\n}\nreturn new ArrayList<>(groups.values());",
    lc: [
      [49, "group-anagrams", "Group Anagrams"],
      [249, "group-shifted-strings", "Group Shifted Strings"],
      [890, "find-and-replace-pattern", "Find and Replace Pattern"],
    ],
  },
  {
    t: "hash",
    n: "Set for O(1) membership",
    tc: "O(n)",
    sc: "O(n)",
    sig: "<b>Signal:</b> 'is x present', deduplicate, or find a sequence/structure where you must test neighbours instantly. The classic tell is <em>longest consecutive sequence in O(n)</em>.",
    why: "Dropping the array into a set makes any neighbour test O(1). The trick that makes it linear rather than quadratic: only start counting a run from a number whose predecessor is absent, so each run is walked exactly once.",
    trap: "Without the <code>v-1 not in s</code> guard this is O(n²) on input like 1..n — it looks linear but re-walks every run from every element. That guard is the entire algorithm.",
    say: "Each element is visited at most twice — once in the scan and once inside the single run it belongs to — so it's O(n), not O(n log n) from sorting.",
    py: "s = set(a)\nbest = 0\nfor v in s:\n    if v - 1 in s:          # not a run start: skip\n        continue\n    length = 1\n    while v + length in s:\n        length += 1\n    best = max(best, length)\nreturn best",
    cpp: "unordered_set<int> s(a.begin(), a.end());\nint best = 0;\nfor (int v : s) {\n    if (s.count(v - 1)) continue;\n    int len = 1;\n    while (s.count(v + len)) len++;\n    best = max(best, len);\n}\nreturn best;",
    java: "Set<Integer> s = new HashSet<>();\nfor (int v : a) s.add(v);\nint best = 0;\nfor (int v : s) {\n    if (s.contains(v - 1)) continue;\n    int len = 1;\n    while (s.contains(v + len)) len++;\n    best = Math.max(best, len);\n}\nreturn best;",
    lc: [
      [128, "longest-consecutive-sequence", "Longest Consecutive Sequence"],
      [217, "contains-duplicate", "Contains Duplicate"],
      [36, "valid-sudoku", "Valid Sudoku"],
    ],
  },
  {
    t: "str",
    n: "Sliding window with counts",
    tc: "O(n)",
    sc: "O(k)",
    sig: "<b>Signal:</b> longest / shortest / count of <em>substrings</em> with a character property — at most k distinct, no repeats, contains all of t, exactly an anagram.",
    why: "Both pointers only move right. Expand to include, shrink only while invalid. Each index enters and leaves once, so 2n moves total despite the nested loop.",
    trap: "Track how many characters currently satisfy the requirement (a <code>have</code> counter) rather than comparing whole maps each step — map comparison inside the loop turns O(n) into O(n·k).",
    say: "Both pointers are monotonically non-decreasing, so the amortised cost is 2n even though it reads as a nested loop.",
    py: "from collections import Counter\nneed = Counter(t)\nhave, want = 0, len(need)\nwin = Counter()\nleft, best = 0, (float('inf'), 0, 0)\nfor right, ch in enumerate(s):\n    win[ch] += 1\n    if ch in need and win[ch] == need[ch]:\n        have += 1\n    while have == want:                   # valid -> try to shrink\n        if right - left + 1 < best[0]:\n            best = (right - left + 1, left, right)\n        c = s[left]\n        win[c] -= 1\n        if c in need and win[c] < need[c]:\n            have -= 1\n        left += 1\nreturn '' if best[0] == float('inf') else s[best[1]:best[2]+1]",
    cpp: "unordered_map<char,int> need, win;\nfor (char c : t) need[c]++;\nint have = 0, want = need.size(), left = 0, bl = 0, bLen = INT_MAX;\nfor (int right = 0; right < (int)s.size(); right++) {\n    char c = s[right];\n    win[c]++;\n    if (need.count(c) && win[c] == need[c]) have++;\n    while (have == want) {\n        if (right - left + 1 < bLen) { bLen = right - left + 1; bl = left; }\n        char d = s[left++];\n        win[d]--;\n        if (need.count(d) && win[d] < need[d]) have--;\n    }\n}\nreturn bLen == INT_MAX ? \"\" : s.substr(bl, bLen);",
    java: "int[] need = new int[128], win = new int[128];\nfor (char c : t.toCharArray()) need[c]++;\nint want = 0;\nfor (int v : need) if (v > 0) want++;\nint have = 0, left = 0, bl = 0, bLen = Integer.MAX_VALUE;\nfor (int right = 0; right < s.length(); right++) {\n    char c = s.charAt(right);\n    if (++win[c] == need[c]) have++;\n    while (have == want) {\n        if (right - left + 1 < bLen) { bLen = right - left + 1; bl = left; }\n        char d = s.charAt(left++);\n        if (win[d]-- == need[d]) have--;\n    }\n}\nreturn bLen == Integer.MAX_VALUE ? \"\" : s.substring(bl, bl + bLen);",
    lc: [
      [3, "longest-substring-without-repeating-characters", "Longest Unique Substring"],
      [76, "minimum-window-substring", "Minimum Window Substring"],
      [424, "longest-repeating-character-replacement", "Char Replacement"],
      [438, "find-all-anagrams-in-a-string", "Find All Anagrams"],
      [567, "permutation-in-string", "Permutation in String"],
    ],
  },
  {
    t: "str",
    n: "Expand around centre",
    tc: "O(n²)",
    sc: "O(1)",
    sig: "<b>Signal:</b> palindromic substrings — longest one, or count them all.",
    why: "Every palindrome has a centre. There are 2n-1 centres (n single characters, n-1 gaps between them). Expand outward from each while the characters match. O(1) space, unlike the O(n²) DP table most people reach for first.",
    trap: "You must run <b>both</b> centre types. Handling only <code>(i, i)</code> silently misses every even-length palindrome like <code>abba</code>, and the code still looks right.",
    say: "2n-1 centres, each expanding O(n), so O(n²) time but O(1) space — better than the DP table, and Manacher's gets it to O(n) if pushed.",
    py: "def expand(s, l, r):\n    while l >= 0 and r < len(s) and s[l] == s[r]:\n        l -= 1; r += 1\n    return s[l+1:r]\n\nbest = ''\nfor i in range(len(s)):\n    for cand in (expand(s, i, i), expand(s, i, i + 1)):\n        if len(cand) > len(best):\n            best = cand\nreturn best",
    cpp: "auto expand = [&](int l, int r) {\n    while (l >= 0 && r < (int)s.size() && s[l] == s[r]) { l--; r++; }\n    return s.substr(l + 1, r - l - 1);\n};\nstring best;\nfor (int i = 0; i < (int)s.size(); i++) {\n    string o = expand(i, i), e = expand(i, i + 1);\n    if (o.size() > best.size()) best = o;\n    if (e.size() > best.size()) best = e;\n}\nreturn best;",
    java: "String best = \"\";\nfor (int i = 0; i < s.length(); i++) {\n    String o = expand(s, i, i), e = expand(s, i, i + 1);\n    if (o.length() > best.length()) best = o;\n    if (e.length() > best.length()) best = e;\n}\nreturn best;\n\nString expand(String s, int l, int r) {\n    while (l >= 0 && r < s.length() && s.charAt(l) == s.charAt(r)) { l--; r++; }\n    return s.substring(l + 1, r);\n}",
    lc: [
      [5, "longest-palindromic-substring", "Longest Palindromic Substring"],
      [647, "palindromic-substrings", "Palindromic Substrings"],
      [131, "palindrome-partitioning", "Palindrome Partitioning"],
    ],
  },
  {
    t: "str",
    n: "String building — never += in a loop",
    tc: "O(n)",
    sc: "O(n)",
    sig: "<b>Signal:</b> you are constructing a string inside a loop. This is not an algorithm, it is the single most common accidental O(n²) in an OA.",
    why: "Strings are immutable, so the textbook claim is that <code>s += c</code> reallocates and copies, making n appends O(n²). That is exactly true in <b>Java</b>. <b>CPython is subtler:</b> it has an in-place resize optimisation that fires when the string has a single reference, so a plain <code>s += c</code> loop usually measures <em>linear</em> — I verified this. But it is fragile: it breaks under PyPy, breaks the moment anything else holds a reference, and does not apply to <code>s = s + a + b</code>. Collect into a list and join once and you never have to reason about it.",
    trap: "Java's <code>String +=</code> inside a loop compiles to a new <code>StringBuilder</code> <em>per iteration</em> — genuinely quadratic, and it looks identical to the fast version. Use one <code>StringBuilder</code> outside the loop. C++ <code>string</code> is mutable and amortised O(1), so the trap does not exist there. The list equivalent <b>is</b> reliably quadratic in every language: <code>out = out + [v]</code> rebuilds the list each time — use <code>append</code>. Measure any of these yourself in the complexity analyser.",
    say: "I'll accumulate into a list and join once, so I do not have to rely on the interpreter's in-place optimisation firing.",
    py: "parts = []\nfor tok in tokens:\n    parts.append(transform(tok))\nreturn ''.join(parts)          # O(n) total\n\n# NOT:  s = ''\n#       for tok in tokens: s += transform(tok)   # O(n^2)",
    cpp: "string out;\nout.reserve(n);                 // optional but free\nfor (auto& tok : tokens) out += transform(tok);\nreturn out;                     // std::string is mutable: already O(n)",
    java: "StringBuilder sb = new StringBuilder();\nfor (String tok : tokens) sb.append(transform(tok));\nreturn sb.toString();          // O(n)\n\n// NOT:  String s = \"\";\n//       for (String t : tokens) s += t;         // O(n^2)",
    lc: [
      [6, "zigzag-conversion", "Zigzag Conversion"],
      [271, "encode-and-decode-strings", "Encode and Decode Strings"],
      [443, "string-compression", "String Compression"],
    ],
  },
  {
    t: "str",
    n: "KMP prefix function",
    tc: "O(n+m)",
    sc: "O(m)",
    sig: "<b>Signal:</b> substring search where the naive O(n·m) will TLE, or anything about the longest proper prefix that is also a suffix (repeated patterns, minimal rotation).",
    why: "pi[i] = length of the longest proper prefix of pattern[0..i] that is also a suffix. On a mismatch you fall back to pi instead of restarting, so the text pointer never moves backwards.",
    trap: "Off-by-one here is brutal and hard to debug under time pressure. If you cannot write it cold, use the built-in (<code>str.find</code>, <code>string::find</code>, <code>indexOf</code>) and say you would use KMP for adversarial input — that reads better than a broken hand-rolled version.",
    say: "The prefix function means the text pointer never backtracks, giving O(n+m) instead of O(n·m).",
    py: "def prefix_function(p):\n    pi = [0] * len(p)\n    k = 0\n    for i in range(1, len(p)):\n        while k and p[i] != p[k]:\n            k = pi[k-1]\n        if p[i] == p[k]:\n            k += 1\n        pi[i] = k\n    return pi\n\n# smallest repeating unit of p:\n#   n - pi[-1]  divides n  ->  that's the period",
    cpp: "vector<int> prefixFunction(const string& p) {\n    vector<int> pi(p.size(), 0);\n    int k = 0;\n    for (int i = 1; i < (int)p.size(); i++) {\n        while (k && p[i] != p[k]) k = pi[k-1];\n        if (p[i] == p[k]) k++;\n        pi[i] = k;\n    }\n    return pi;\n}",
    java: "int[] prefixFunction(String p) {\n    int[] pi = new int[p.length()];\n    int k = 0;\n    for (int i = 1; i < p.length(); i++) {\n        while (k > 0 && p.charAt(i) != p.charAt(k)) k = pi[k-1];\n        if (p.charAt(i) == p.charAt(k)) k++;\n        pi[i] = k;\n    }\n    return pi;\n}",
    lc: [
      [28, "find-the-index-of-the-first-occurrence-in-a-string", "Find First Occurrence"],
      [459, "repeated-substring-pattern", "Repeated Substring Pattern"],
      [214, "shortest-palindrome", "Shortest Palindrome"],
    ],
  },
  {
    t: "ll",
    n: "Dummy head node",
    tc: "O(n)",
    sc: "O(1)",
    sig: "<b>Signal:</b> any list problem where the <em>head itself might change</em> — deleting the first node, inserting before it, merging, partitioning.",
    why: "A sentinel node in front means the head is never a special case. You write one uniform loop instead of an if-branch that everyone forgets and then debugs for five minutes.",
    trap: "Return <code>dummy.next</code>, not <code>dummy</code>, and not your saved <code>head</code> — the original head may no longer be first.",
    say: "The dummy removes the head as a special case, so there's exactly one code path instead of two.",
    py: "dummy = ListNode(0, head)\nprev = dummy\nwhile prev.next:\n    if should_delete(prev.next):\n        prev.next = prev.next.next     # unlink, don't advance\n    else:\n        prev = prev.next\nreturn dummy.next",
    cpp: "ListNode dummy(0);\ndummy.next = head;\nListNode* prev = &dummy;\nwhile (prev->next) {\n    if (shouldDelete(prev->next)) prev->next = prev->next->next;\n    else                          prev = prev->next;\n}\nreturn dummy.next;",
    java: "ListNode dummy = new ListNode(0);\ndummy.next = head;\nListNode prev = dummy;\nwhile (prev.next != null) {\n    if (shouldDelete(prev.next)) prev.next = prev.next.next;\n    else                         prev = prev.next;\n}\nreturn dummy.next;",
    lc: [
      [203, "remove-linked-list-elements", "Remove List Elements"],
      [19, "remove-nth-node-from-end-of-list", "Remove Nth From End"],
      [86, "partition-list", "Partition List"],
      [2, "add-two-numbers", "Add Two Numbers"],
    ],
  },
  {
    t: "ll",
    n: "Iterative reversal",
    tc: "O(n)",
    sc: "O(1)",
    sig: "<b>Signal:</b> reverse the list, or any sub-range of it. Also the second half of palindrome-check and reorder problems.",
    why: "Three pointers walking forward, flipping one link per step. Save next before you overwrite the pointer — that single line is the whole algorithm.",
    trap: "Losing <code>nxt</code> before reassigning <code>cur.next</code> orphans the rest of the list instantly. Also: the recursive version is O(n) <em>stack</em> and will overflow on 10⁵ nodes in an OA.",
    say: "O(1) space iteratively; the recursive version is elegant but O(n) stack and will blow up on large inputs.",
    py: "prev, cur = None, head\nwhile cur:\n    nxt = cur.next      # save FIRST\n    cur.next = prev     # flip\n    prev = cur          # advance\n    cur = nxt\nreturn prev             # new head",
    cpp: "ListNode *prev = nullptr, *cur = head;\nwhile (cur) {\n    ListNode* nxt = cur->next;\n    cur->next = prev;\n    prev = cur;\n    cur = nxt;\n}\nreturn prev;",
    java: "ListNode prev = null, cur = head;\nwhile (cur != null) {\n    ListNode nxt = cur.next;\n    cur.next = prev;\n    prev = cur;\n    cur = nxt;\n}\nreturn prev;",
    lc: [
      [206, "reverse-linked-list", "Reverse Linked List"],
      [92, "reverse-linked-list-ii", "Reverse Linked List II"],
      [234, "palindrome-linked-list", "Palindrome Linked List"],
      [25, "reverse-nodes-in-k-group", "Reverse in k-Group"],
    ],
  },
  {
    t: "ll",
    n: "Fast & slow pointers",
    tc: "O(n)",
    sc: "O(1)",
    sig: "<b>Signal:</b> detect a cycle, find the middle, or find the k-th from the end — in one pass with no extra memory.",
    why: "Two pointers at different speeds. If a cycle exists the fast one laps the slow one and they must meet. For the entry point: after they meet, reset one to the head and advance both one step — they meet exactly at the cycle entrance.",
    trap: "Guard <b>both</b> <code>fast</code> and <code>fast.next</code> before the double step, or an even-length list dereferences null. Also note that <code>slow</code> lands on the <em>second</em> middle for even lengths — if you need the first, start fast at <code>head.next</code>.",
    say: "Floyd's gives cycle detection in O(1) space where the obvious hash-set solution costs O(n).",
    py: "slow = fast = head\nwhile fast and fast.next:\n    slow = slow.next\n    fast = fast.next.next\n    if slow is fast:\n        break\nelse:\n    return None                 # no cycle\n\nslow = head                     # find the entry\nwhile slow is not fast:\n    slow, fast = slow.next, fast.next\nreturn slow",
    cpp: "ListNode *slow = head, *fast = head;\nwhile (fast && fast->next) {\n    slow = slow->next;\n    fast = fast->next->next;\n    if (slow == fast) break;\n}\nif (!fast || !fast->next) return nullptr;\nslow = head;\nwhile (slow != fast) { slow = slow->next; fast = fast->next; }\nreturn slow;",
    java: "ListNode slow = head, fast = head;\nwhile (fast != null && fast.next != null) {\n    slow = slow.next;\n    fast = fast.next.next;\n    if (slow == fast) break;\n}\nif (fast == null || fast.next == null) return null;\nslow = head;\nwhile (slow != fast) { slow = slow.next; fast = fast.next; }\nreturn slow;",
    lc: [
      [141, "linked-list-cycle", "Linked List Cycle"],
      [142, "linked-list-cycle-ii", "Cycle II"],
      [876, "middle-of-the-linked-list", "Middle of List"],
      [287, "find-the-duplicate-number", "Find the Duplicate"],
    ],
  },
  {
    t: "ll",
    n: "Merge two sorted lists",
    tc: "O(n+m)",
    sc: "O(1)",
    sig: "<b>Signal:</b> merge sorted lists, or the merge step inside a linked-list merge sort (the O(1)-space way to sort a list).",
    why: "Walk both with a tail pointer, always attaching the smaller head. Because you are relinking rather than copying, it costs no extra memory.",
    trap: "Do not forget to attach the remaining tail — <code>tail.next = l1 or l2</code>. Dropping that line silently truncates the result and only fails on unbalanced inputs.",
    say: "Relinking rather than copying keeps it O(1) space, which is why merge sort on a linked list beats it on an array for memory.",
    py: "dummy = ListNode(0)\ntail = dummy\nwhile l1 and l2:\n    if l1.val <= l2.val:\n        tail.next, l1 = l1, l1.next\n    else:\n        tail.next, l2 = l2, l2.next\n    tail = tail.next\ntail.next = l1 or l2        # attach the remainder\nreturn dummy.next",
    cpp: "ListNode dummy(0); ListNode* tail = &dummy;\nwhile (l1 && l2) {\n    if (l1->val <= l2->val) { tail->next = l1; l1 = l1->next; }\n    else                    { tail->next = l2; l2 = l2->next; }\n    tail = tail->next;\n}\ntail->next = l1 ? l1 : l2;\nreturn dummy.next;",
    java: "ListNode dummy = new ListNode(0), tail = dummy;\nwhile (l1 != null && l2 != null) {\n    if (l1.val <= l2.val) { tail.next = l1; l1 = l1.next; }\n    else                  { tail.next = l2; l2 = l2.next; }\n    tail = tail.next;\n}\ntail.next = (l1 != null) ? l1 : l2;\nreturn dummy.next;",
    lc: [
      [21, "merge-two-sorted-lists", "Merge Two Sorted Lists"],
      [23, "merge-k-sorted-lists", "Merge k Sorted Lists"],
      [148, "sort-list", "Sort List"],
    ],
  },
  {
    t: "stk",
    n: "Monotonic stack",
    tc: "O(n)",
    sc: "O(n)",
    sig: "<b>Signal:</b> for each element, the next/previous greater or smaller element. Spans, histograms, daily temperatures, trapped water, stock spans.",
    why: "Keep the stack monotonic. When a bigger value arrives it is the answer for everything you pop. Each index is pushed once and popped once — 2n operations — so it is O(n) despite the inner while.",
    trap: "Push <b>indices</b>, not values — you almost always need the distance. And decide <code>&lt;</code> versus <code>&lt;=</code> deliberately: with equal elements that choice flips next-greater into next-greater-or-equal and silently changes the answer on histogram problems.",
    say: "Every index is pushed and popped at most once, so it's amortised O(n) even though the inner loop looks quadratic.",
    py: "stack, out = [], [-1] * len(a)     # stack holds INDICES\nfor i, v in enumerate(a):\n    while stack and a[stack[-1]] < v:\n        out[stack.pop()] = i        # v is the next greater\n    stack.append(i)\nreturn out                          # anything left has none",
    cpp: "vector<int> st, out(a.size(), -1);\nfor (int i = 0; i < (int)a.size(); i++) {\n    while (!st.empty() && a[st.back()] < a[i]) {\n        out[st.back()] = i;\n        st.pop_back();\n    }\n    st.push_back(i);\n}\nreturn out;",
    java: "Deque<Integer> st = new ArrayDeque<>();\nint[] out = new int[a.length];\nArrays.fill(out, -1);\nfor (int i = 0; i < a.length; i++) {\n    while (!st.isEmpty() && a[st.peek()] < a[i])\n        out[st.pop()] = i;\n    st.push(i);\n}\nreturn out;",
    lc: [
      [739, "daily-temperatures", "Daily Temperatures"],
      [496, "next-greater-element-i", "Next Greater Element I"],
      [84, "largest-rectangle-in-histogram", "Largest Rectangle"],
      [42, "trapping-rain-water", "Trapping Rain Water"],
      [85, "maximal-rectangle", "Maximal Rectangle"],
    ],
  },
  {
    t: "stk",
    n: "Balanced brackets",
    tc: "O(n)",
    sc: "O(n)",
    sig: "<b>Signal:</b> matching pairs, nesting validity, expression parsing, or anything where the most recent unclosed thing matters.",
    why: "A stack is exactly 'the most recently opened thing that is still open'. Push openers, and on a closer check that the top matches.",
    trap: "Two failure modes, not one: a closer arriving on an <em>empty</em> stack, and a <em>non-empty</em> stack at the end. Most people check one and lose the hidden test for the other.",
    say: "Nesting is inherently LIFO, which is precisely what a stack models.",
    py: "pairs = {')': '(', ']': '[', '}': '{'}\nstack = []\nfor c in s:\n    if c in pairs.values():\n        stack.append(c)\n    elif c in pairs:\n        if not stack or stack.pop() != pairs[c]:\n            return False\nreturn not stack        # must be EMPTY at the end",
    cpp: "unordered_map<char,char> pairs{{')','('},{']','['},{'}','{'}};\nvector<char> st;\nfor (char c : s) {\n    if (c=='('||c=='['||c=='{') st.push_back(c);\n    else if (pairs.count(c)) {\n        if (st.empty() || st.back() != pairs[c]) return false;\n        st.pop_back();\n    }\n}\nreturn st.empty();",
    java: "Deque<Character> st = new ArrayDeque<>();\nfor (char c : s.toCharArray()) {\n    if (c=='('||c=='['||c=='{') st.push(c);\n    else {\n        if (st.isEmpty()) return false;\n        char o = st.pop();\n        if ((c==')'&&o!='(') || (c==']'&&o!='[') || (c=='}'&&o!='{'))\n            return false;\n    }\n}\nreturn st.isEmpty();",
    lc: [
      [20, "valid-parentheses", "Valid Parentheses"],
      [32, "longest-valid-parentheses", "Longest Valid Parentheses"],
      [150, "evaluate-reverse-polish-notation", "Evaluate RPN"],
      [394, "decode-string", "Decode String"],
    ],
  },
  {
    t: "stk",
    n: "Monotonic deque (window max)",
    tc: "O(n)",
    sc: "O(k)",
    sig: "<b>Signal:</b> maximum or minimum of every sliding window of fixed size k.",
    why: "Keep a deque of indices whose values are decreasing. The front is always the window maximum. Pop from the back anything smaller than the incoming value (it can never be the max again), and pop from the front anything that has slid out of range.",
    trap: "A heap gives O(n log k) and is accepted, but the deque is the intended O(n). Store indices so you can test the front for expiry — storing values makes eviction impossible.",
    say: "Each index enters and leaves the deque once, so it's O(n) — a heap would be O(n log k).",
    py: "from collections import deque\ndq, out = deque(), []          # indices, values decreasing\nfor i, v in enumerate(a):\n    while dq and a[dq[-1]] <= v:\n        dq.pop()               # smaller: never the max again\n    dq.append(i)\n    if dq[0] <= i - k:\n        dq.popleft()           # slid out of the window\n    if i >= k - 1:\n        out.append(a[dq[0]])\nreturn out",
    cpp: "deque<int> dq; vector<int> out;\nfor (int i = 0; i < (int)a.size(); i++) {\n    while (!dq.empty() && a[dq.back()] <= a[i]) dq.pop_back();\n    dq.push_back(i);\n    if (dq.front() <= i - k) dq.pop_front();\n    if (i >= k - 1) out.push_back(a[dq.front()]);\n}\nreturn out;",
    java: "Deque<Integer> dq = new ArrayDeque<>();\nint[] out = new int[a.length - k + 1];\nfor (int i = 0; i < a.length; i++) {\n    while (!dq.isEmpty() && a[dq.peekLast()] <= a[i]) dq.pollLast();\n    dq.addLast(i);\n    if (dq.peekFirst() <= i - k) dq.pollFirst();\n    if (i >= k - 1) out[i - k + 1] = a[dq.peekFirst()];\n}\nreturn out;",
    lc: [
      [239, "sliding-window-maximum", "Sliding Window Maximum"],
      [862, "shortest-subarray-with-sum-at-least-k", "Shortest Subarray Sum ≥ K"],
      [1438, "longest-continuous-subarray-with-absolute-diff-less-than-or-equal-to-limit", "Abs Diff ≤ Limit"],
    ],
  },
  {
    t: "stk",
    n: "Auxiliary stack (min-stack)",
    tc: "O(1) per op",
    sc: "O(n)",
    sig: "<b>Signal:</b> a stack that must also report its minimum/maximum in O(1), or any 'design a structure with O(1) X' question.",
    why: "Carry the answer alongside the data. Each pushed element remembers the minimum of everything at or below it, so popping restores the previous minimum for free.",
    trap: "When pushing a duplicate of the current minimum you must push it to the min-stack too — otherwise popping one copy loses the minimum while a copy is still present.",
    say: "I'm trading O(n) extra space to make the min query O(1) instead of O(n).",
    py: "class MinStack:\n    def __init__(self):\n        self.st, self.mins = [], []\n    def push(self, v):\n        self.st.append(v)\n        # <=  so duplicates of the min are recorded too\n        if not self.mins or v <= self.mins[-1]:\n            self.mins.append(v)\n    def pop(self):\n        if self.st.pop() == self.mins[-1]:\n            self.mins.pop()\n    def getMin(self):\n        return self.mins[-1]",
    cpp: "class MinStack {\n    vector<int> st, mins;\npublic:\n    void push(int v) {\n        st.push_back(v);\n        if (mins.empty() || v <= mins.back()) mins.push_back(v);\n    }\n    void pop() {\n        if (st.back() == mins.back()) mins.pop_back();\n        st.pop_back();\n    }\n    int getMin() { return mins.back(); }\n};",
    java: "class MinStack {\n    private Deque<Integer> st = new ArrayDeque<>();\n    private Deque<Integer> mins = new ArrayDeque<>();\n    public void push(int v) {\n        st.push(v);\n        if (mins.isEmpty() || v <= mins.peek()) mins.push(v);\n    }\n    public void pop() {\n        if (st.pop().equals(mins.peek())) mins.pop();\n    }\n    public int getMin() { return mins.peek(); }\n}",
    lc: [
      [155, "min-stack", "Min Stack"],
      [232, "implement-queue-using-stacks", "Queue using Stacks"],
      [716, "max-stack", "Max Stack"],
    ],
  },
  {
    t: "tree",
    n: "DFS traversal (the three orders)",
    tc: "O(n)",
    sc: "O(h)",
    sig: "<b>Signal:</b> almost any tree question. Choose the order by <em>when</em> you need the node relative to its children.",
    why: "Pre-order visits the node before its subtrees (copying, serialising). In-order on a BST yields sorted output. Post-order visits children first, which is what you need whenever a node's answer depends on its subtrees' answers.",
    trap: "Recursion depth is O(h). A skewed tree of 10⁵ nodes blows Python's default 1000-deep limit and Java's stack. In an OA either raise the limit (<code>sys.setrecursionlimit</code>) or go iterative.",
    say: "Post-order is the one to reach for when a node's value depends on its children — that's most 'compute something about the subtree' problems.",
    py: "def dfs(node):\n    if not node: return\n    # PRE-order:  visit(node)\n    dfs(node.left)\n    # IN-order:   visit(node)   -> sorted for a BST\n    dfs(node.right)\n    # POST-order: visit(node)   -> children already done\n\n# iterative in-order (no recursion limit)\nstack, cur, out = [], root, []\nwhile stack or cur:\n    while cur:\n        stack.append(cur); cur = cur.left\n    cur = stack.pop()\n    out.append(cur.val)\n    cur = cur.right",
    cpp: "void dfs(TreeNode* n) {\n    if (!n) return;\n    dfs(n->left);\n    dfs(n->right);\n}\n\n// iterative in-order\nvector<int> out; stack<TreeNode*> st; TreeNode* cur = root;\nwhile (!st.empty() || cur) {\n    while (cur) { st.push(cur); cur = cur->left; }\n    cur = st.top(); st.pop();\n    out.push_back(cur->val);\n    cur = cur->right;\n}",
    java: "void dfs(TreeNode n) {\n    if (n == null) return;\n    dfs(n.left);\n    dfs(n.right);\n}\n\n// iterative in-order\nList<Integer> out = new ArrayList<>();\nDeque<TreeNode> st = new ArrayDeque<>();\nTreeNode cur = root;\nwhile (!st.isEmpty() || cur != null) {\n    while (cur != null) { st.push(cur); cur = cur.left; }\n    cur = st.pop();\n    out.add(cur.val);\n    cur = cur.right;\n}",
    lc: [
      [94, "binary-tree-inorder-traversal", "Inorder Traversal"],
      [144, "binary-tree-preorder-traversal", "Preorder Traversal"],
      [145, "binary-tree-postorder-traversal", "Postorder Traversal"],
      [230, "kth-smallest-element-in-a-bst", "Kth Smallest in BST"],
    ],
  },
  {
    t: "tree",
    n: "BFS level order",
    tc: "O(n)",
    sc: "O(w)",
    sig: "<b>Signal:</b> anything mentioning <em>levels</em>, <em>depth</em>, <em>shortest path in an unweighted structure</em>, or a right/left side view.",
    why: "Process the queue one full level at a time by snapshotting its size before the inner loop. That snapshot is what separates level-order from a flat BFS.",
    trap: "Capture <code>len(queue)</code> <b>before</b> the inner loop. Reading it inside means you consume nodes you just enqueued and the levels merge into one.",
    say: "Snapshotting the queue length per level is what gives you level boundaries for free.",
    py: "from collections import deque\nif not root: return []\nq, out = deque([root]), []\nwhile q:\n    n = len(q)                  # snapshot BEFORE the loop\n    level = []\n    for _ in range(n):\n        node = q.popleft()\n        level.append(node.val)\n        if node.left:  q.append(node.left)\n        if node.right: q.append(node.right)\n    out.append(level)\nreturn out",
    cpp: "vector<vector<int>> out;\nif (!root) return out;\nqueue<TreeNode*> q; q.push(root);\nwhile (!q.empty()) {\n    int n = q.size();\n    vector<int> level;\n    for (int i = 0; i < n; i++) {\n        TreeNode* node = q.front(); q.pop();\n        level.push_back(node->val);\n        if (node->left)  q.push(node->left);\n        if (node->right) q.push(node->right);\n    }\n    out.push_back(level);\n}\nreturn out;",
    java: "List<List<Integer>> out = new ArrayList<>();\nif (root == null) return out;\nQueue<TreeNode> q = new ArrayDeque<>();\nq.add(root);\nwhile (!q.isEmpty()) {\n    int n = q.size();\n    List<Integer> level = new ArrayList<>();\n    for (int i = 0; i < n; i++) {\n        TreeNode node = q.poll();\n        level.add(node.val);\n        if (node.left != null)  q.add(node.left);\n        if (node.right != null) q.add(node.right);\n    }\n    out.add(level);\n}\nreturn out;",
    lc: [
      [102, "binary-tree-level-order-traversal", "Level Order Traversal"],
      [199, "binary-tree-right-side-view", "Right Side View"],
      [103, "binary-tree-zigzag-level-order-traversal", "Zigzag Level Order"],
      [111, "minimum-depth-of-binary-tree", "Minimum Depth"],
    ],
  },
  {
    t: "tree",
    n: "Bottom-up subtree aggregate",
    tc: "O(n)",
    sc: "O(h)",
    sig: "<b>Signal:</b> height, diameter, balanced check, maximum path sum, largest BST subtree — anything where a node's answer needs its children's answers.",
    why: "Post-order recursion returns a value up the tree while updating a global best on the way. One traversal computes both the local return value and the global answer, which is what makes the naive O(n²) version linear.",
    trap: "Distinguish the <b>return value</b> from the <b>global answer</b> — they differ. For diameter you return height but record height(l)+height(r). Returning the diameter itself is the classic wrong answer. For max-path-sum you must also clamp negatives with <code>max(0, child)</code>.",
    say: "One post-order pass computes the answer for every subtree, so it's O(n) rather than the O(n²) you'd get from recomputing height at every node.",
    py: "best = 0\ndef height(node):\n    nonlocal best\n    if not node: return 0\n    l = height(node.left)\n    r = height(node.right)\n    best = max(best, l + r)      # GLOBAL: path through this node\n    return 1 + max(l, r)         # RETURN: height, not diameter\nheight(root)\nreturn best",
    cpp: "int best = 0;\nfunction<int(TreeNode*)> height = [&](TreeNode* n) -> int {\n    if (!n) return 0;\n    int l = height(n->left), r = height(n->right);\n    best = max(best, l + r);\n    return 1 + max(l, r);\n};\nheight(root);\nreturn best;",
    java: "private int best = 0;\nint height(TreeNode n) {\n    if (n == null) return 0;\n    int l = height(n.left), r = height(n.right);\n    best = Math.max(best, l + r);\n    return 1 + Math.max(l, r);\n}\n// call height(root); then return best;",
    lc: [
      [543, "diameter-of-binary-tree", "Diameter of Binary Tree"],
      [124, "binary-tree-maximum-path-sum", "Max Path Sum"],
      [110, "balanced-binary-tree", "Balanced Binary Tree"],
      [104, "maximum-depth-of-binary-tree", "Maximum Depth"],
    ],
  },
  {
    t: "tree",
    n: "BST invariant (range validation & search)",
    tc: "O(h)",
    sc: "O(h)",
    sig: "<b>Signal:</b> the word <em>binary search tree</em>. Validation, insert, delete, k-th smallest, closest value.",
    why: "Every node is bounded by a range inherited from its ancestors, not just by its parent. Pass (low, high) down and narrow it at each step.",
    trap: "Checking only <code>left.val &lt; node.val &lt; right.val</code> is <b>wrong</b> — it accepts trees where a deep left-subtree node exceeds a distant ancestor. You must carry the full range down. Also use long/None bounds so <code>Integer.MIN_VALUE</code> as an actual node value doesn't break it.",
    say: "The BST property is about ancestors, not parents, so I'll thread a valid range down the recursion rather than compare locally.",
    py: "def valid(node, lo=float('-inf'), hi=float('inf')):\n    if not node: return True\n    if not (lo < node.val < hi): return False\n    return (valid(node.left,  lo, node.val) and\n            valid(node.right, node.val, hi))\n\n# in-order of a BST is sorted -> kth smallest is the kth visited",
    cpp: "bool valid(TreeNode* n, long lo = LONG_MIN, long hi = LONG_MAX) {\n    if (!n) return true;\n    if (n->val <= lo || n->val >= hi) return false;\n    return valid(n->left, lo, n->val) &&\n           valid(n->right, n->val, hi);\n}",
    java: "boolean valid(TreeNode n, long lo, long hi) {\n    if (n == null) return true;\n    if (n.val <= lo || n.val >= hi) return false;\n    return valid(n.left, lo, n.val) &&\n           valid(n.right, n.val, hi);\n}\n// call: valid(root, Long.MIN_VALUE, Long.MAX_VALUE)",
    lc: [
      [98, "validate-binary-search-tree", "Validate BST"],
      [230, "kth-smallest-element-in-a-bst", "Kth Smallest"],
      [701, "insert-into-a-binary-search-tree", "Insert into BST"],
      [450, "delete-node-in-a-bst", "Delete Node in BST"],
    ],
  },
  {
    t: "tree",
    n: "Lowest common ancestor",
    tc: "O(n)",
    sc: "O(h)",
    sig: "<b>Signal:</b> lowest common ancestor, distance between two nodes, or the deepest node covering a set.",
    why: "Recurse both sides. If both return non-null, the current node is the split point and therefore the LCA. If only one returns non-null, pass it up. In a BST you skip the recursion entirely: walk down while both targets are on the same side.",
    trap: "The general-tree version assumes both nodes actually exist. If they might not, you must also verify presence, otherwise you return an ancestor of only one node and it looks correct.",
    say: "The first node where the two searches diverge is by definition the lowest common ancestor.",
    py: "def lca(node, p, q):\n    if not node or node is p or node is q:\n        return node\n    l = lca(node.left, p, q)\n    r = lca(node.right, p, q)\n    if l and r: return node      # split point\n    return l or r\n\n# BST version, O(h), no recursion:\n# while (root.val - p.val) * (root.val - q.val) > 0:\n#     root = root.left if p.val < root.val else root.right",
    cpp: "TreeNode* lca(TreeNode* n, TreeNode* p, TreeNode* q) {\n    if (!n || n == p || n == q) return n;\n    TreeNode* l = lca(n->left, p, q);\n    TreeNode* r = lca(n->right, p, q);\n    if (l && r) return n;\n    return l ? l : r;\n}",
    java: "TreeNode lca(TreeNode n, TreeNode p, TreeNode q) {\n    if (n == null || n == p || n == q) return n;\n    TreeNode l = lca(n.left, p, q);\n    TreeNode r = lca(n.right, p, q);\n    if (l != null && r != null) return n;\n    return l != null ? l : r;\n}",
    lc: [
      [236, "lowest-common-ancestor-of-a-binary-tree", "LCA of Binary Tree"],
      [235, "lowest-common-ancestor-of-a-binary-search-tree", "LCA of BST"],
      [1123, "lowest-common-ancestor-of-deepest-leaves", "LCA of Deepest Leaves"],
    ],
  },
  {
    t: "heap",
    n: "Bounded heap for top-k",
    tc: "O(n log k)",
    sc: "O(k)",
    sig: "<b>Signal:</b> top k, k-th largest/smallest, k closest. You need partial order and never the fully sorted array.",
    why: "Sorting is O(n log n) and answers far more than was asked. Keep a heap of size k, evicting whenever it overflows — O(n log k), which is effectively linear when k is small.",
    trap: "For the k-th <b>largest</b> you need a <b>min</b>-heap (so the weakest survivor sits on top and gets evicted) and vice versa. Getting this backwards is the classic slip. Java's <code>PriorityQueue</code> is a min-heap by default; C++'s <code>priority_queue</code> is a <b>max</b>-heap by default.",
    say: "A bounded heap is O(n log k), not O(n log n) — and quickselect gets true O(n) expected if I'm allowed to mutate the input.",
    py: "import heapq\nheap = []                       # min-heap of size k\nfor v in a:\n    heapq.heappush(heap, v)\n    if len(heap) > k:\n        heapq.heappop(heap)     # drop the smallest\nreturn heap[0]                  # kth LARGEST\n\n# one-liners: heapq.nlargest(k, a)  /  heapq.nsmallest(k, a)",
    cpp: "priority_queue<int, vector<int>, greater<int>> pq;   // MIN-heap\nfor (int v : a) {\n    pq.push(v);\n    if ((int)pq.size() > k) pq.pop();\n}\nreturn pq.top();\n\n// O(n) expected, mutates the input:\n// nth_element(a.begin(), a.begin() + k - 1, a.end(), greater<int>());",
    java: "PriorityQueue<Integer> pq = new PriorityQueue<>();  // min-heap default\nfor (int v : a) {\n    pq.offer(v);\n    if (pq.size() > k) pq.poll();\n}\nreturn pq.peek();",
    lc: [
      [215, "kth-largest-element-in-an-array", "Kth Largest Element"],
      [347, "top-k-frequent-elements", "Top K Frequent"],
      [973, "k-closest-points-to-origin", "K Closest Points"],
      [703, "kth-largest-element-in-a-stream", "Kth Largest in Stream"],
    ],
  },
  {
    t: "heap",
    n: "Two heaps (running median)",
    tc: "O(log n) insert",
    sc: "O(n)",
    sig: "<b>Signal:</b> median of a stream, or maintaining a balanced split around a moving centre.",
    why: "A max-heap holds the lower half, a min-heap the upper half. The median sits at one or both tops. Rebalance so the sizes never differ by more than one.",
    trap: "Always push through one heap and pop across to the other, then rebalance — pushing directly to whichever side 'looks right' breaks the invariant on ties. Also decide up front whether the low heap is allowed the extra element.",
    say: "Both tops are O(1), so the median is O(1) to read and O(log n) to maintain.",
    py: "import heapq\nlow, high = [], []          # low: max-heap (negated), high: min-heap\n\ndef add(v):\n    heapq.heappush(low, -v)                     # always via low\n    heapq.heappush(high, -heapq.heappop(low))   # move top across\n    if len(high) > len(low):                    # rebalance\n        heapq.heappush(low, -heapq.heappop(high))\n\ndef median():\n    if len(low) > len(high): return -low[0]\n    return (-low[0] + high[0]) / 2",
    cpp: "priority_queue<int> low;                              // max-heap\npriority_queue<int, vector<int>, greater<int>> high;   // min-heap\n\nvoid add(int v) {\n    low.push(v);\n    high.push(low.top()); low.pop();\n    if (high.size() > low.size()) { low.push(high.top()); high.pop(); }\n}\ndouble median() {\n    if (low.size() > high.size()) return low.top();\n    return (low.top() + high.top()) / 2.0;\n}",
    java: "PriorityQueue<Integer> low  = new PriorityQueue<>(Collections.reverseOrder());\nPriorityQueue<Integer> high = new PriorityQueue<>();\n\nvoid add(int v) {\n    low.offer(v);\n    high.offer(low.poll());\n    if (high.size() > low.size()) low.offer(high.poll());\n}\ndouble median() {\n    if (low.size() > high.size()) return low.peek();\n    return (low.peek() + high.peek()) / 2.0;\n}",
    lc: [
      [295, "find-median-from-data-stream", "Find Median from Data Stream"],
      [480, "sliding-window-median", "Sliding Window Median"],
      [502, "ipo", "IPO"],
    ],
  },
  {
    t: "heap",
    n: "Merge k sorted sequences",
    tc: "O(n log k)",
    sc: "O(k)",
    sig: "<b>Signal:</b> merge k sorted lists/arrays, or repeatedly take the smallest across several sources.",
    why: "Hold one candidate from each source in a heap of size k. Pop the global minimum and push that source's next element. You only ever compare k things, never n.",
    trap: "Push a tiebreaker (the source index) alongside the value, or Python will try to compare the objects themselves and raise <code>TypeError</code> when two values are equal. Java needs an explicit comparator for the same reason.",
    say: "The heap only ever holds k items, so it's O(n log k) rather than O(n log n) from concatenating and sorting.",
    py: "import heapq\nheap = []\nfor i, lst in enumerate(lists):\n    if lst:\n        # (value, source index, position) - index breaks ties\n        heapq.heappush(heap, (lst[0], i, 0))\nout = []\nwhile heap:\n    val, i, j = heapq.heappop(heap)\n    out.append(val)\n    if j + 1 < len(lists[i]):\n        heapq.heappush(heap, (lists[i][j+1], i, j+1))\nreturn out",
    cpp: "using T = tuple<int,int,int>;              // value, list, index\npriority_queue<T, vector<T>, greater<T>> pq;\nfor (int i = 0; i < (int)lists.size(); i++)\n    if (!lists[i].empty()) pq.push({lists[i][0], i, 0});\nvector<int> out;\nwhile (!pq.empty()) {\n    auto [v, i, j] = pq.top(); pq.pop();\n    out.push_back(v);\n    if (j + 1 < (int)lists[i].size())\n        pq.push({lists[i][j+1], i, j+1});\n}\nreturn out;",
    java: "PriorityQueue<int[]> pq = new PriorityQueue<>((x, y) -> x[0] - y[0]);\nfor (int i = 0; i < lists.length; i++)\n    if (lists[i].length > 0) pq.offer(new int[]{lists[i][0], i, 0});\nList<Integer> out = new ArrayList<>();\nwhile (!pq.isEmpty()) {\n    int[] cur = pq.poll();\n    out.add(cur[0]);\n    if (cur[2] + 1 < lists[cur[1]].length)\n        pq.offer(new int[]{lists[cur[1]][cur[2]+1], cur[1], cur[2]+1});\n}\nreturn out;",
    lc: [
      [23, "merge-k-sorted-lists", "Merge k Sorted Lists"],
      [378, "kth-smallest-element-in-a-sorted-matrix", "Kth Smallest in Matrix"],
      [632, "smallest-range-covering-elements-from-k-lists", "Smallest Range"],
    ],
  },
  {
    t: "graph",
    n: "BFS — shortest path, unweighted",
    tc: "O(V+E)",
    sc: "O(V)",
    sig: "<b>Signal:</b> fewest steps / minimum moves / shortest path where <b>every edge costs the same</b>. Word ladders, mazes, knight moves.",
    why: "BFS reaches every node in non-decreasing distance order, so the first time you touch a node you have found its shortest path. No relaxation needed — that is why Dijkstra is overkill here.",
    trap: "Mark visited <b>when you enqueue</b>, not when you dequeue. Marking on dequeue lets the same node enter the queue many times and degrades to exponential on dense graphs. This is the single most common BFS bug.",
    say: "All edges are unit weight, so BFS gives the shortest path in O(V+E) — Dijkstra's log factor buys nothing here.",
    py: "from collections import deque\nq = deque([(start, 0)])\nseen = {start}                     # mark on ENQUEUE\nwhile q:\n    node, d = q.popleft()\n    if node == target:\n        return d\n    for nxt in graph[node]:\n        if nxt not in seen:\n            seen.add(nxt)          # <- here, not after popleft\n            q.append((nxt, d + 1))\nreturn -1",
    cpp: "queue<pair<int,int>> q;\nvector<bool> seen(n, false);\nq.push({start, 0}); seen[start] = true;\nwhile (!q.empty()) {\n    auto [node, d] = q.front(); q.pop();\n    if (node == target) return d;\n    for (int nxt : graph[node])\n        if (!seen[nxt]) { seen[nxt] = true; q.push({nxt, d + 1}); }\n}\nreturn -1;",
    java: "Queue<int[]> q = new ArrayDeque<>();\nboolean[] seen = new boolean[n];\nq.add(new int[]{start, 0}); seen[start] = true;\nwhile (!q.isEmpty()) {\n    int[] cur = q.poll();\n    if (cur[0] == target) return cur[1];\n    for (int nxt : graph.get(cur[0]))\n        if (!seen[nxt]) { seen[nxt] = true; q.add(new int[]{nxt, cur[1] + 1}); }\n}\nreturn -1;",
    lc: [
      [127, "word-ladder", "Word Ladder"],
      [542, "01-matrix", "01 Matrix"],
      [994, "rotting-oranges", "Rotting Oranges"],
      [1091, "shortest-path-in-binary-matrix", "Shortest Path in Binary Matrix"],
    ],
  },
  {
    t: "graph",
    n: "Grid as an implicit graph",
    tc: "O(R·C)",
    sc: "O(R·C)",
    sig: "<b>Signal:</b> a 2D grid of islands, regions, rot spreading, flood fill. There is no explicit graph — the grid <em>is</em> the graph.",
    why: "Each cell is a node; its neighbours are the 4 (or 8) adjacent cells. Once you see that, every grid problem is just BFS or DFS with a bounds check.",
    trap: "Bounds-check <b>before</b> indexing, not after. And for multi-source spread (rotting oranges, nearest exit) push <em>all</em> sources into the queue at the start — running BFS once per source is O(n²) and times out.",
    say: "The grid is an implicit graph with R·C nodes and 4 edges each, so BFS/DFS is O(R·C).",
    py: "DIRS = ((1,0), (-1,0), (0,1), (0,-1))\nR, C = len(grid), len(grid[0])\n\ndef dfs(r, c):\n    if r < 0 or r >= R or c < 0 or c >= C: return\n    if grid[r][c] != '1': return\n    grid[r][c] = '0'               # mark visited in place\n    for dr, dc in DIRS:\n        dfs(r + dr, c + dc)\n\nislands = 0\nfor r in range(R):\n    for c in range(C):\n        if grid[r][c] == '1':\n            islands += 1\n            dfs(r, c)",
    cpp: "int DR[4] = {1,-1,0,0}, DC[4] = {0,0,1,-1};\nint R = grid.size(), C = grid[0].size();\n\nfunction<void(int,int)> dfs = [&](int r, int c) {\n    if (r < 0 || r >= R || c < 0 || c >= C) return;\n    if (grid[r][c] != '1') return;\n    grid[r][c] = '0';\n    for (int k = 0; k < 4; k++) dfs(r + DR[k], c + DC[k]);\n};\nint islands = 0;\nfor (int r = 0; r < R; r++)\n    for (int c = 0; c < C; c++)\n        if (grid[r][c] == '1') { islands++; dfs(r, c); }",
    java: "int[] DR = {1,-1,0,0}, DC = {0,0,1,-1};\n\nvoid dfs(char[][] g, int r, int c) {\n    if (r < 0 || r >= g.length || c < 0 || c >= g[0].length) return;\n    if (g[r][c] != '1') return;\n    g[r][c] = '0';\n    for (int k = 0; k < 4; k++) dfs(g, r + DR[k], c + DC[k]);\n}\n\nint islands = 0;\nfor (int r = 0; r < grid.length; r++)\n    for (int c = 0; c < grid[0].length; c++)\n        if (grid[r][c] == '1') { islands++; dfs(grid, r, c); }",
    lc: [
      [200, "number-of-islands", "Number of Islands"],
      [994, "rotting-oranges", "Rotting Oranges"],
      [733, "flood-fill", "Flood Fill"],
      [695, "max-area-of-island", "Max Area of Island"],
      [130, "surrounded-regions", "Surrounded Regions"],
    ],
  },
  {
    t: "graph",
    n: "Topological sort (Kahn's)",
    tc: "O(V+E)",
    sc: "O(V)",
    sig: "<b>Signal:</b> ordering with prerequisites — course schedules, build dependencies, task ordering. Also the standard way to <em>detect a cycle</em> in a directed graph.",
    why: "Repeatedly emit any node with in-degree zero and decrement its neighbours. If you emit fewer than V nodes, the leftovers form a cycle — so ordering and cycle detection are the same algorithm.",
    trap: "The cycle check is the point: <code>len(order) != V</code> means a cycle exists. Forgetting it is why 'Course Schedule' fails. Build the in-degree array from the edge list <em>before</em> you start, not lazily.",
    say: "Kahn's gives me the ordering and cycle detection in one pass — if the output is short, there's a cycle.",
    py: "from collections import deque\nindeg = [0] * n\nfor u, v in edges:            # edge u -> v\n    indeg[v] += 1\nq = deque(i for i in range(n) if indeg[i] == 0)\norder = []\nwhile q:\n    u = q.popleft()\n    order.append(u)\n    for v in graph[u]:\n        indeg[v] -= 1\n        if indeg[v] == 0:\n            q.append(v)\nreturn order if len(order) == n else []   # short -> cycle",
    cpp: "vector<int> indeg(n, 0);\nfor (auto& e : edges) indeg[e[1]]++;\nqueue<int> q;\nfor (int i = 0; i < n; i++) if (!indeg[i]) q.push(i);\nvector<int> order;\nwhile (!q.empty()) {\n    int u = q.front(); q.pop();\n    order.push_back(u);\n    for (int v : graph[u])\n        if (--indeg[v] == 0) q.push(v);\n}\nreturn (int)order.size() == n ? order : vector<int>{};",
    java: "int[] indeg = new int[n];\nfor (int[] e : edges) indeg[e[1]]++;\nQueue<Integer> q = new ArrayDeque<>();\nfor (int i = 0; i < n; i++) if (indeg[i] == 0) q.add(i);\nList<Integer> order = new ArrayList<>();\nwhile (!q.isEmpty()) {\n    int u = q.poll();\n    order.add(u);\n    for (int v : graph.get(u))\n        if (--indeg[v] == 0) q.add(v);\n}\nreturn order.size() == n ? order : new ArrayList<>();",
    lc: [
      [207, "course-schedule", "Course Schedule"],
      [210, "course-schedule-ii", "Course Schedule II"],
      [269, "alien-dictionary", "Alien Dictionary"],
      [310, "minimum-height-trees", "Minimum Height Trees"],
    ],
  },
  {
    t: "graph",
    n: "Union-Find (DSU)",
    tc: "O(α(n)) ≈ O(1)",
    sc: "O(n)",
    sig: "<b>Signal:</b> connected components, 'are these two in the same group', redundant edges, Kruskal's MST, accounts merge. Especially when edges arrive <em>incrementally</em>.",
    why: "Each set has a representative. Union links two roots; find walks to the root while flattening the path. With path compression plus union by rank the amortised cost is the inverse Ackermann function — effectively constant.",
    trap: "Without <b>both</b> path compression and union by rank you get O(log n) or worse and it will TLE on 10⁵ unions. Also: union returns false when the two are already connected — that is exactly how you detect a redundant edge or a cycle.",
    say: "With path compression and union by rank it's O(α(n)) amortised, which is constant for any input that fits in memory.",
    py: "parent = list(range(n))\nrank = [0] * n\n\ndef find(x):\n    while parent[x] != x:\n        parent[x] = parent[parent[x]]   # path compression\n        x = parent[x]\n    return x\n\ndef union(a, b):\n    ra, rb = find(a), find(b)\n    if ra == rb: return False           # already connected\n    if rank[ra] < rank[rb]: ra, rb = rb, ra\n    parent[rb] = ra\n    if rank[ra] == rank[rb]: rank[ra] += 1\n    return True",
    cpp: "vector<int> parent(n), rnk(n, 0);\niota(parent.begin(), parent.end(), 0);\n\nfunction<int(int)> find = [&](int x) {\n    while (parent[x] != x) { parent[x] = parent[parent[x]]; x = parent[x]; }\n    return x;\n};\nauto unite = [&](int a, int b) {\n    int ra = find(a), rb = find(b);\n    if (ra == rb) return false;\n    if (rnk[ra] < rnk[rb]) swap(ra, rb);\n    parent[rb] = ra;\n    if (rnk[ra] == rnk[rb]) rnk[ra]++;\n    return true;\n};",
    java: "int[] parent = new int[n], rank = new int[n];\nfor (int i = 0; i < n; i++) parent[i] = i;\n\nint find(int x) {\n    while (parent[x] != x) { parent[x] = parent[parent[x]]; x = parent[x]; }\n    return x;\n}\nboolean union(int a, int b) {\n    int ra = find(a), rb = find(b);\n    if (ra == rb) return false;\n    if (rank[ra] < rank[rb]) { int t = ra; ra = rb; rb = t; }\n    parent[rb] = ra;\n    if (rank[ra] == rank[rb]) rank[ra]++;\n    return true;\n}",
    lc: [
      [547, "number-of-provinces", "Number of Provinces"],
      [684, "redundant-connection", "Redundant Connection"],
      [721, "accounts-merge", "Accounts Merge"],
      [1584, "min-cost-to-connect-all-points", "Min Cost to Connect Points"],
      [128, "longest-consecutive-sequence", "Longest Consecutive Sequence"],
    ],
  },
  {
    t: "graph",
    n: "Dijkstra (weighted shortest path)",
    tc: "O((V+E) log V)",
    sc: "O(V)",
    sig: "<b>Signal:</b> shortest path with <b>non-negative</b> edge weights — cheapest flight, minimum effort path, network delay time.",
    why: "A greedy BFS ordered by distance. Pop the closest unfinalised node; since all weights are non-negative, its distance can never improve later, so it is final.",
    trap: "Two traps. First, <b>negative weights break it</b> — use Bellman-Ford instead. Second, the lazy version pushes duplicates, so you must skip stale entries with <code>if d &gt; dist[u]: continue</code>; without that line the complexity is wrong and it can TLE.",
    say: "Non-negative weights mean the first time I pop a node its distance is final — that's the greedy exchange argument Dijkstra rests on.",
    py: "import heapq\ndist = [float('inf')] * n\ndist[src] = 0\npq = [(0, src)]                       # (distance, node)\nwhile pq:\n    d, u = heapq.heappop(pq)\n    if d > dist[u]:\n        continue                      # stale entry - MUST skip\n    for v, w in graph[u]:\n        nd = d + w\n        if nd < dist[v]:\n            dist[v] = nd\n            heapq.heappush(pq, (nd, v))\nreturn dist",
    cpp: "vector<long long> dist(n, LLONG_MAX);\ndist[src] = 0;\npriority_queue<pair<long long,int>, vector<pair<long long,int>>,\n               greater<>> pq;\npq.push({0, src});\nwhile (!pq.empty()) {\n    auto [d, u] = pq.top(); pq.pop();\n    if (d > dist[u]) continue;\n    for (auto& [v, w] : graph[u])\n        if (d + w < dist[v]) {\n            dist[v] = d + w;\n            pq.push({dist[v], v});\n        }\n}",
    java: "long[] dist = new long[n];\nArrays.fill(dist, Long.MAX_VALUE);\ndist[src] = 0;\nPriorityQueue<long[]> pq = new PriorityQueue<>((x,y) -> Long.compare(x[0], y[0]));\npq.add(new long[]{0, src});\nwhile (!pq.isEmpty()) {\n    long[] cur = pq.poll();\n    int u = (int) cur[1];\n    if (cur[0] > dist[u]) continue;\n    for (int[] e : graph.get(u))\n        if (cur[0] + e[1] < dist[e[0]]) {\n            dist[e[0]] = cur[0] + e[1];\n            pq.add(new long[]{dist[e[0]], e[0]});\n        }\n}",
    lc: [
      [743, "network-delay-time", "Network Delay Time"],
      [1631, "path-with-minimum-effort", "Path With Minimum Effort"],
      [787, "cheapest-flights-within-k-stops", "Cheapest Flights K Stops"],
      [1514, "path-with-maximum-probability", "Path with Max Probability"],
    ],
  },
  {
    t: "graph",
    n: "Cycle detection & bipartite check",
    tc: "O(V+E)",
    sc: "O(V)",
    sig: "<b>Signal:</b> 'is this a valid tree', 'can it be 2-coloured', 'are there conflicting constraints', deadlock detection.",
    why: "For a <em>directed</em> graph, a cycle is a back-edge to a node currently on the recursion stack — you need three states, not two. For an <em>undirected</em> graph, any edge to a visited node that is not your parent is a cycle. Bipartite is the same DFS with alternating colours.",
    trap: "Directed graphs need <b>three</b> states (unvisited / in-progress / done). Using a plain boolean visited array reports false cycles on diamond shapes, which is a very common wrong answer.",
    say: "Directed cycles need the in-progress state — a plain visited flag can't distinguish a back-edge from a cross-edge.",
    py: "WHITE, GRAY, BLACK = 0, 1, 2\nstate = [WHITE] * n\n\ndef has_cycle(u):\n    state[u] = GRAY                    # on the recursion stack\n    for v in graph[u]:\n        if state[v] == GRAY:           # back edge -> cycle\n            return True\n        if state[v] == WHITE and has_cycle(v):\n            return True\n    state[u] = BLACK\n    return False\n\n# bipartite: colour[v] = 1 - colour[u]; conflict -> not bipartite",
    cpp: "vector<int> state(n, 0);           // 0 white, 1 gray, 2 black\nfunction<bool(int)> hasCycle = [&](int u) {\n    state[u] = 1;\n    for (int v : graph[u]) {\n        if (state[v] == 1) return true;\n        if (state[v] == 0 && hasCycle(v)) return true;\n    }\n    state[u] = 2;\n    return false;\n};",
    java: "int[] state = new int[n];          // 0 white, 1 gray, 2 black\nboolean hasCycle(int u) {\n    state[u] = 1;\n    for (int v : graph.get(u)) {\n        if (state[v] == 1) return true;\n        if (state[v] == 0 && hasCycle(v)) return true;\n    }\n    state[u] = 2;\n    return false;\n}",
    lc: [
      [207, "course-schedule", "Course Schedule"],
      [785, "is-graph-bipartite", "Is Graph Bipartite"],
      [261, "graph-valid-tree", "Graph Valid Tree"],
      [802, "find-eventual-safe-states", "Eventual Safe States"],
    ],
  },
  {
    t: "arr",
    n: "Two pointers, converging",
    tc: "O(n)",
    sc: "O(1)",
    sig: "<b>Signal:</b> sorted array, find a pair meeting a numeric condition. Also container-with-water and trapping-rain-water shapes.",
    why: "Sortedness makes the sum monotone. If a[lo]+a[hi] is too small, a[hi] is already the largest partner available for a[lo], so no pair using a[lo] can work — lo is eliminated, not merely skipped.",
    trap: "If the problem wants original indices, sorting destroys them — sort (value, index) pairs or switch to the hash-map approach. For 3Sum you need <b>two</b> dedup skips: one on the pivot, one on lo after a hit.",
    say: "The discard argument is the proof: when the sum is too small, every pair involving a[lo] is too small.",
    py: "lo, hi = 0, len(a) - 1\nwhile lo < hi:\n    s = a[lo] + a[hi]\n    if s == target: return lo, hi\n    if s < target:  lo += 1     # only lo can raise the sum\n    else:           hi -= 1     # only hi can lower it\nreturn None",
    cpp: "int lo = 0, hi = a.size() - 1;\nwhile (lo < hi) {\n    int s = a[lo] + a[hi];\n    if (s == target) return {lo, hi};\n    if (s < target) lo++;\n    else            hi--;\n}\nreturn {-1, -1};",
    java: "int lo = 0, hi = a.length - 1;\nwhile (lo < hi) {\n    int s = a[lo] + a[hi];\n    if (s == target) return new int[]{lo, hi};\n    if (s < target) lo++;\n    else            hi--;\n}\nreturn new int[]{-1, -1};",
    lc: [
      [167, "two-sum-ii-input-array-is-sorted", "Two Sum II"],
      [15, "3sum", "3Sum"],
      [11, "container-with-most-water", "Container With Most Water"],
      [42, "trapping-rain-water", "Trapping Rain Water"],
    ],
  },
  {
    t: "arr",
    n: "Kadane (max subarray)",
    tc: "O(n)",
    sc: "O(1)",
    sig: "<b>Signal:</b> optimal <em>contiguous</em> sum or product, values may be negative, no length constraint.",
    why: "The best subarray ending at i is either a[i] alone or the best ending at i-1 extended. Nothing else is possible, so the DP table collapses to two scalars.",
    trap: "Seeding <code>best = 0</code> returns 0 on an all-negative array instead of the least-negative element. Seed from <code>a[0]</code>. For max <em>product</em> you must track the minimum too, because a negative times a negative flips it to the maximum.",
    say: "O(1) state per index, so this is a DP whose table degenerates to two variables.",
    py: "best = cur = a[0]\nfor i in range(1, len(a)):\n    cur  = max(a[i], cur + a[i])   # restart, or extend\n    best = max(best, cur)\nreturn best",
    cpp: "int best = a[0], cur = a[0];\nfor (int i = 1; i < (int)a.size(); i++) {\n    cur  = max(a[i], cur + a[i]);\n    best = max(best, cur);\n}\nreturn best;",
    java: "int best = a[0], cur = a[0];\nfor (int i = 1; i < a.length; i++) {\n    cur  = Math.max(a[i], cur + a[i]);\n    best = Math.max(best, cur);\n}\nreturn best;",
    lc: [
      [53, "maximum-subarray", "Maximum Subarray"],
      [152, "maximum-product-subarray", "Max Product Subarray"],
      [918, "maximum-sum-circular-subarray", "Circular Max Sum"],
    ],
  },
  {
    t: "arr",
    n: "In-place read / write pointers",
    tc: "O(n)",
    sc: "O(1)",
    sig: "<b>Signal:</b> remove, filter or compact in place while preserving relative order, with O(1) extra space.",
    why: "One pointer reads every element; a slower pointer marks where the next kept element belongs. Because write never overtakes read, overwriting in place is always safe.",
    trap: "Do not delete while iterating instead. <code>a.pop(i)</code> in a loop is O(n) per call — quietly O(n²) — and it also skips the element that slid into the vacated slot.",
    say: "Write never overtakes read, which is the invariant that makes O(1) space safe here.",
    py: "write = 0\nfor read in range(len(a)):\n    if keep(a[read]):\n        a[write] = a[read]\n        write += 1\nreturn write            # new logical length",
    cpp: "int write = 0;\nfor (int read = 0; read < (int)a.size(); read++)\n    if (keep(a[read])) a[write++] = a[read];\nreturn write;",
    java: "int write = 0;\nfor (int read = 0; read < a.length; read++)\n    if (keep(a[read])) a[write++] = a[read];\nreturn write;",
    lc: [
      [26, "remove-duplicates-from-sorted-array", "Remove Duplicates"],
      [27, "remove-element", "Remove Element"],
      [283, "move-zeroes", "Move Zeroes"],
      [80, "remove-duplicates-from-sorted-array-ii", "Remove Duplicates II"],
    ],
  },
  {
    t: "arr",
    n: "Cyclic sort (index as hash)",
    tc: "O(n)",
    sc: "O(1)",
    sig: "<b>Signal:</b> values are a permutation of 1..n or 0..n and you must find the missing or duplicate one in O(1) space.",
    why: "When values and indices share a range, the array is its own hash table. Put every value v at index v-1; whichever index disagrees with its value is the answer.",
    trap: "Guard with <code>a[i] != a[j]</code>, not <code>i != j</code> — with duplicates the latter spins forever swapping two equal values. And do not advance i after a swap; the value you just received is unplaced.",
    say: "Values and indices share a range, so the array doubles as an O(1)-space hash table.",
    py: "i = 0\nwhile i < len(a):\n    j = a[i] - 1                          # where a[i] belongs\n    if 0 <= j < len(a) and a[i] != a[j]:\n        a[i], a[j] = a[j], a[i]           # swap, do NOT advance\n    else:\n        i += 1\nfor i, v in enumerate(a):\n    if v != i + 1: return i + 1\nreturn len(a) + 1",
    cpp: "int i = 0, n = a.size();\nwhile (i < n) {\n    int j = a[i] - 1;\n    if (j >= 0 && j < n && a[i] != a[j]) swap(a[i], a[j]);\n    else i++;\n}\nfor (int k = 0; k < n; k++)\n    if (a[k] != k + 1) return k + 1;\nreturn n + 1;",
    java: "int i = 0, n = a.length;\nwhile (i < n) {\n    int j = a[i] - 1;\n    if (j >= 0 && j < n && a[i] != a[j]) { int t = a[i]; a[i] = a[j]; a[j] = t; }\n    else i++;\n}\nfor (int k = 0; k < n; k++)\n    if (a[k] != k + 1) return k + 1;\nreturn n + 1;",
    lc: [
      [41, "first-missing-positive", "First Missing Positive"],
      [268, "missing-number", "Missing Number"],
      [448, "find-all-numbers-disappeared-in-an-array", "Disappeared Numbers"],
      [442, "find-all-duplicates-in-an-array", "All Duplicates"],
    ],
  },
  {
    t: "arr",
    n: "Difference array (range updates)",
    tc: "O(1) update",
    sc: "O(n)",
    sig: "<b>Signal:</b> thousands of range updates — add v to every index in [l, r] — and you only read the array at the end.",
    why: "The inverse of prefix sums. Record only where the increment starts and stops, then integrate once. Each update touches two cells instead of r-l+1.",
    trap: "Size the array <code>n+1</code>, not <code>n</code>, or the <code>d[r+1]</code> write on the last index is out of bounds. And the moment you need reads interleaved with updates, you need a Fenwick tree instead.",
    say: "A difference array is the discrete derivative; the final pass is the integral — O(1) per update instead of O(n).",
    py: "d = [0] * (n + 1)             # n+1, not n\nfor l, r, v in updates:\n    d[l]     += v\n    d[r + 1] -= v\n\nfrom itertools import accumulate\nfinal = list(accumulate(d))[:n]",
    cpp: "vector<long long> d(n + 1, 0);\nfor (auto& u : updates) {\n    d[u[0]]     += u[2];\n    d[u[1] + 1] -= u[2];\n}\nfor (int i = 1; i < n; i++) d[i] += d[i-1];",
    java: "long[] d = new long[n + 1];\nfor (int[] u : updates) {\n    d[u[0]]     += u[2];\n    d[u[1] + 1] -= u[2];\n}\nfor (int i = 1; i < n; i++) d[i] += d[i-1];",
    lc: [
      [1109, "corporate-flight-bookings", "Corporate Flight Bookings"],
      [370, "range-addition", "Range Addition"],
      [1094, "car-pooling", "Car Pooling"],
      [2381, "shifting-letters-ii", "Shifting Letters II"],
    ],
  },
  {
    t: "arr",
    n: "Merge intervals / sweep line",
    tc: "O(n log n)",
    sc: "O(n)",
    sig: "<b>Signal:</b> overlapping ranges — merge them, count concurrency, minimum removals to make disjoint, meeting rooms.",
    why: "Sort by start and merge adjacent overlaps in one pass. For concurrency, split each interval into a +1 event at start and -1 at end, sort, and sweep — the running total is the answer.",
    trap: "Ties decide the answer. With half-open intervals a meeting ending at 10 does not conflict with one starting at 10, so the -1 event must sort first. Sorting <code>(time, delta)</code> handles this for free because -1 &lt; +1; sorting by time alone does not.",
    say: "Once sorted by start, any overlap must be with the immediately preceding interval — that's what makes one linear pass sufficient.",
    py: "ivs.sort(key=lambda x: x[0])\nout = [ivs[0]]\nfor s, e in ivs[1:]:\n    if s <= out[-1][1]:\n        out[-1][1] = max(out[-1][1], e)\n    else:\n        out.append([s, e])\n\n# max concurrency - sweep line\nev = [(s, 1) for s, e in ivs] + [(e, -1) for s, e in ivs]\nev.sort()                    # -1 sorts before +1 at ties\ncur = best = 0\nfor _, d in ev:\n    cur += d\n    best = max(best, cur)",
    cpp: "sort(ivs.begin(), ivs.end());\nvector<vector<int>> out{ivs[0]};\nfor (int i = 1; i < (int)ivs.size(); i++) {\n    if (ivs[i][0] <= out.back()[1])\n        out.back()[1] = max(out.back()[1], ivs[i][1]);\n    else\n        out.push_back(ivs[i]);\n}\nreturn out;",
    java: "Arrays.sort(ivs, (x, y) -> Integer.compare(x[0], y[0]));\nList<int[]> out = new ArrayList<>();\nout.add(ivs[0]);\nfor (int i = 1; i < ivs.length; i++) {\n    int[] last = out.get(out.size() - 1);\n    if (ivs[i][0] <= last[1]) last[1] = Math.max(last[1], ivs[i][1]);\n    else out.add(ivs[i]);\n}\nreturn out.toArray(new int[0][]);",
    lc: [
      [56, "merge-intervals", "Merge Intervals"],
      [57, "insert-interval", "Insert Interval"],
      [435, "non-overlapping-intervals", "Non-overlapping Intervals"],
      [253, "meeting-rooms-ii", "Meeting Rooms II"],
      [452, "minimum-number-of-arrows-to-burst-balloons", "Burst Balloons"],
    ],
  },
  {
    t: "bs",
    n: "Lower bound (the only template)",
    tc: "O(log n)",
    sc: "O(1)",
    sig: "<b>Signal:</b> genuinely sorted data — find a value, its first occurrence, or the insertion point.",
    why: "Keep the answer inside [lo, hi). Never write mid-1 on the side that might hold the answer. That single discipline removes every off-by-one.",
    trap: "The mirror template is <b>not</b> interchangeable. If you move <code>lo = mid</code>, you must round the midpoint <b>up</b> (<code>lo + (hi-lo+1)/2</code>) or it infinite-loops the moment hi = lo+1. Knowing only one template is the most common binary search failure.",
    say: "lower_bound also returns the insertion point, so one function covers membership, first occurrence, count, and where to insert.",
    py: "# FIRST index with a[i] >= target\nlo, hi = 0, len(a)          # hi EXCLUSIVE\nwhile lo < hi:\n    mid = lo + (hi - lo) // 2\n    if a[mid] < target: lo = mid + 1\n    else:               hi = mid\nreturn lo\n\n# MIRROR: LAST index where pred(i) is true\nlo, hi = 0, len(a) - 1\nwhile lo < hi:\n    mid = lo + (hi - lo + 1) // 2    # CEILING, mandatory\n    if pred(mid): lo = mid\n    else:         hi = mid - 1",
    cpp: "int lo = 0, hi = a.size();\nwhile (lo < hi) {\n    int mid = lo + (hi - lo) / 2;   // never (lo+hi)/2: overflow\n    if (a[mid] < target) lo = mid + 1;\n    else                 hi = mid;\n}\nreturn lo;\n\n// stdlib: lower_bound(a.begin(), a.end(), target) - a.begin();",
    java: "int lo = 0, hi = a.length;\nwhile (lo < hi) {\n    int mid = lo + (hi - lo) / 2;\n    if (a[mid] < target) lo = mid + 1;\n    else                 hi = mid;\n}\nreturn lo;\n\n// Arrays.binarySearch returns -(insertion point) - 1 when absent",
    lc: [
      [704, "binary-search", "Binary Search"],
      [35, "search-insert-position", "Search Insert Position"],
      [34, "find-first-and-last-position-of-element-in-sorted-array", "First and Last Position"],
      [658, "find-k-closest-elements", "K Closest Elements"],
    ],
  },
  {
    t: "bs",
    n: "Binary search on the answer",
    tc: "O(n log range)",
    sc: "O(1)",
    sig: "<b>Signal:</b> minimise the maximum, maximise the minimum, smallest capacity/speed/days that works. If you hear <em>just barely enough</em>, it is this.",
    why: "You search the answer space, not the array. Write a boolean feasible(x); if it is monotone — true above a threshold, false below — binary search the threshold. Most candidates miss it because there is no sorted array in sight.",
    trap: "Verify monotonicity first. If feasible flips true/false/true, binary search returns nonsense confidently. Set <code>hi</code> to a genuinely achievable bound (usually <code>sum(a)</code> or <code>max(a)</code>), not an arbitrary large constant.",
    say: "The array isn't what's sorted — the predicate is monotone over the answer space, which is all binary search actually needs.",
    py: "def feasible(x):\n    # can we do it with capacity / speed / limit x?\n    total, cur = 1, 0\n    for v in a:\n        if cur + v > x:\n            total += 1\n            cur = 0\n        cur += v\n    return total <= k\n\nlo, hi = max(a), sum(a)      # achievable bounds\nwhile lo < hi:\n    mid = lo + (hi - lo) // 2\n    if feasible(mid): hi = mid       # works, try smaller\n    else:             lo = mid + 1\nreturn lo",
    cpp: "auto feasible = [&](long long x) {\n    long long total = 1, cur = 0;\n    for (int v : a) {\n        if (cur + v > x) { total++; cur = 0; }\n        cur += v;\n    }\n    return total <= k;\n};\nlong long lo = *max_element(a.begin(), a.end());\nlong long hi = accumulate(a.begin(), a.end(), 0LL);\nwhile (lo < hi) {\n    long long mid = lo + (hi - lo) / 2;\n    if (feasible(mid)) hi = mid;\n    else               lo = mid + 1;\n}\nreturn lo;",
    java: "long lo = 0, hi = 0;\nfor (int v : a) { lo = Math.max(lo, v); hi += v; }\nwhile (lo < hi) {\n    long mid = lo + (hi - lo) / 2;\n    if (feasible(mid)) hi = mid;\n    else               lo = mid + 1;\n}\nreturn (int) lo;",
    lc: [
      [875, "koko-eating-bananas", "Koko Eating Bananas"],
      [1011, "capacity-to-ship-packages-within-d-days", "Ship Packages in D Days"],
      [410, "split-array-largest-sum", "Split Array Largest Sum"],
      [1482, "minimum-number-of-days-to-make-m-bouquets", "Bouquets"],
      [2226, "maximum-candies-allocated-to-k-children", "Maximum Candies"],
    ],
  },
  {
    t: "bs",
    n: "Rotated sorted array",
    tc: "O(log n)",
    sc: "O(1)",
    sig: "<b>Signal:</b> a sorted array rotated at an unknown pivot.",
    why: "At any midpoint at least one half is still perfectly sorted, and comparing a[mid] to a[lo] tells you which. Check whether the target lies inside that sorted half; recurse there if so, otherwise the other.",
    trap: "With duplicates allowed, <code>a[lo] == a[mid]</code> tells you nothing about which half is sorted and the worst case degrades to O(n). LC 81 and 154 exist purely to test whether you know that.",
    say: "The invariant is that at least one half is always sorted, which is what keeps the halving valid.",
    py: "lo, hi = 0, len(a) - 1\nwhile lo <= hi:\n    mid = lo + (hi - lo) // 2\n    if a[mid] == target: return mid\n    if a[lo] <= a[mid]:                    # left half sorted\n        if a[lo] <= target < a[mid]: hi = mid - 1\n        else:                        lo = mid + 1\n    else:                                  # right half sorted\n        if a[mid] < target <= a[hi]: lo = mid + 1\n        else:                        hi = mid - 1\nreturn -1",
    cpp: "int lo = 0, hi = a.size() - 1;\nwhile (lo <= hi) {\n    int mid = lo + (hi - lo) / 2;\n    if (a[mid] == target) return mid;\n    if (a[lo] <= a[mid]) {\n        if (a[lo] <= target && target < a[mid]) hi = mid - 1;\n        else                                    lo = mid + 1;\n    } else {\n        if (a[mid] < target && target <= a[hi]) lo = mid + 1;\n        else                                    hi = mid - 1;\n    }\n}\nreturn -1;",
    java: "int lo = 0, hi = a.length - 1;\nwhile (lo <= hi) {\n    int mid = lo + (hi - lo) / 2;\n    if (a[mid] == target) return mid;\n    if (a[lo] <= a[mid]) {\n        if (a[lo] <= target && target < a[mid]) hi = mid - 1;\n        else                                    lo = mid + 1;\n    } else {\n        if (a[mid] < target && target <= a[hi]) lo = mid + 1;\n        else                                    hi = mid - 1;\n    }\n}\nreturn -1;",
    lc: [
      [33, "search-in-rotated-sorted-array", "Search in Rotated Array"],
      [153, "find-minimum-in-rotated-sorted-array", "Find Minimum"],
      [81, "search-in-rotated-sorted-array-ii", "With Duplicates"],
      [4, "median-of-two-sorted-arrays", "Median of Two Sorted Arrays"],
    ],
  },
  {
    t: "dp",
    n: "1D linear DP",
    tc: "O(n)",
    sc: "O(1)",
    sig: "<b>Signal:</b> the answer at position i depends on a fixed number of earlier positions. Climbing stairs, house robber, decode ways, min cost climbing.",
    why: "Define dp[i] as the answer for the prefix ending at i, write the recurrence, then notice you only ever look back a constant distance — so the array collapses to two or three variables.",
    trap: "Get the base cases right; almost all failures here are dp[0] and dp[1], not the recurrence. Write the recurrence on paper with n=1,2,3 before coding.",
    say: "The recurrence only looks back a constant distance, so I can drop the table and keep O(1) space.",
    py: "# house robber: rob i, or skip it\nprev2, prev1 = 0, 0\nfor v in a:\n    prev2, prev1 = prev1, max(prev1, prev2 + v)\nreturn prev1",
    cpp: "int prev2 = 0, prev1 = 0;\nfor (int v : a) {\n    int cur = max(prev1, prev2 + v);\n    prev2 = prev1;\n    prev1 = cur;\n}\nreturn prev1;",
    java: "int prev2 = 0, prev1 = 0;\nfor (int v : a) {\n    int cur = Math.max(prev1, prev2 + v);\n    prev2 = prev1;\n    prev1 = cur;\n}\nreturn prev1;",
    lc: [
      [70, "climbing-stairs", "Climbing Stairs"],
      [198, "house-robber", "House Robber"],
      [213, "house-robber-ii", "House Robber II"],
      [91, "decode-ways", "Decode Ways"],
      [746, "min-cost-climbing-stairs", "Min Cost Climbing Stairs"],
    ],
  },
  {
    t: "dp",
    n: "0/1 knapsack",
    tc: "O(n·W)",
    sc: "O(W)",
    sig: "<b>Signal:</b> pick a subset under a capacity limit, each item usable <b>once</b>. Partition into equal subsets, target sum, last stone weight.",
    why: "dp[w] = best achievable with capacity w. Process items in the outer loop and capacities in the <em>reverse</em> inner loop, so each item is considered at most once.",
    trap: "The loop direction <b>is</b> the algorithm. Descending capacity = 0/1 (each item once). Ascending = unbounded (unlimited copies). Swap them by accident and you silently solve a different problem that still compiles and often passes the samples.",
    say: "Iterating capacity downwards is what enforces using each item at most once — ascending would allow reuse.",
    py: "dp = [False] * (target + 1)\ndp[0] = True\nfor v in nums:\n    for w in range(target, v - 1, -1):   # DESCENDING = 0/1\n        dp[w] = dp[w] or dp[w - v]\nreturn dp[target]",
    cpp: "vector<bool> dp(target + 1, false);\ndp[0] = true;\nfor (int v : nums)\n    for (int w = target; w >= v; w--)      // DESCENDING\n        dp[w] = dp[w] || dp[w - v];\nreturn dp[target];",
    java: "boolean[] dp = new boolean[target + 1];\ndp[0] = true;\nfor (int v : nums)\n    for (int w = target; w >= v; w--)      // DESCENDING\n        dp[w] = dp[w] || dp[w - v];\nreturn dp[target];",
    lc: [
      [416, "partition-equal-subset-sum", "Partition Equal Subset Sum"],
      [494, "target-sum", "Target Sum"],
      [1049, "last-stone-weight-ii", "Last Stone Weight II"],
      [474, "ones-and-zeroes", "Ones and Zeroes"],
    ],
  },
  {
    t: "dp",
    n: "Unbounded knapsack / coin change",
    tc: "O(n·A)",
    sc: "O(A)",
    sig: "<b>Signal:</b> unlimited copies of each item. Coin change, combination sum count, integer break, perfect squares.",
    why: "Same table as 0/1, but capacity ascends so an item can be reused within the same pass.",
    trap: "Loop order changes the <em>meaning</em>. Coins outside / amount inside counts <b>combinations</b> (unordered). Amount outside / coins inside counts <b>permutations</b> (ordered). LC 518 wants the first, LC 377 the second — same code, swapped loops, different answers.",
    say: "Ascending capacity permits reuse; and the nesting order decides whether I'm counting combinations or permutations.",
    py: "# minimum coins\ndp = [float('inf')] * (amount + 1)\ndp[0] = 0\nfor c in coins:\n    for w in range(c, amount + 1):     # ASCENDING = unbounded\n        dp[w] = min(dp[w], dp[w - c] + 1)\nreturn -1 if dp[amount] == float('inf') else dp[amount]\n\n# counting COMBINATIONS: coins outer, amount inner\n# counting PERMUTATIONS: amount outer, coins inner",
    cpp: "vector<int> dp(amount + 1, INT_MAX);\ndp[0] = 0;\nfor (int c : coins)\n    for (int w = c; w <= amount; w++)   // ASCENDING\n        if (dp[w - c] != INT_MAX)\n            dp[w] = min(dp[w], dp[w - c] + 1);\nreturn dp[amount] == INT_MAX ? -1 : dp[amount];",
    java: "int[] dp = new int[amount + 1];\nArrays.fill(dp, Integer.MAX_VALUE);\ndp[0] = 0;\nfor (int c : coins)\n    for (int w = c; w <= amount; w++)\n        if (dp[w - c] != Integer.MAX_VALUE)\n            dp[w] = Math.min(dp[w], dp[w - c] + 1);\nreturn dp[amount] == Integer.MAX_VALUE ? -1 : dp[amount];",
    lc: [
      [322, "coin-change", "Coin Change"],
      [518, "coin-change-ii", "Coin Change II (combinations)"],
      [377, "combination-sum-iv", "Combination Sum IV (permutations)"],
      [279, "perfect-squares", "Perfect Squares"],
    ],
  },
  {
    t: "dp",
    n: "Longest increasing subsequence",
    tc: "O(n log n)",
    sc: "O(n)",
    sig: "<b>Signal:</b> longest increasing/decreasing subsequence, Russian doll envelopes, maximum chain of pairs.",
    why: "The O(n²) DP is obvious. The O(n log n) version keeps <code>tails[k]</code> = smallest possible tail of an increasing subsequence of length k+1. That array is always sorted, so each element is placed with one binary search.",
    trap: "<code>tails</code> is <b>not</b> a valid subsequence — only its <em>length</em> is the answer. Printing it as the LIS is wrong. For non-strict (allow equal) use <code>bisect_right</code> instead of <code>bisect_left</code>.",
    say: "tails stays sorted by construction, so each insertion is a binary search — O(n log n) rather than the O(n²) DP.",
    py: "from bisect import bisect_left\ntails = []\nfor v in a:\n    i = bisect_left(tails, v)     # bisect_right for non-strict\n    if i == len(tails):\n        tails.append(v)\n    else:\n        tails[i] = v\nreturn len(tails)                 # LENGTH only, not the LIS",
    cpp: "vector<int> tails;\nfor (int v : a) {\n    auto it = lower_bound(tails.begin(), tails.end(), v);\n    if (it == tails.end()) tails.push_back(v);\n    else                   *it = v;\n}\nreturn tails.size();",
    java: "List<Integer> tails = new ArrayList<>();\nfor (int v : a) {\n    int i = Collections.binarySearch(tails, v);\n    if (i < 0) i = -(i + 1);\n    if (i == tails.size()) tails.add(v);\n    else                   tails.set(i, v);\n}\nreturn tails.size();",
    lc: [
      [300, "longest-increasing-subsequence", "Longest Increasing Subsequence"],
      [354, "russian-doll-envelopes", "Russian Doll Envelopes"],
      [646, "maximum-length-of-pair-chain", "Maximum Length of Pair Chain"],
      [1671, "minimum-number-of-removals-to-make-mountain-array", "Mountain Array Removals"],
    ],
  },
  {
    t: "dp",
    n: "Grid DP",
    tc: "O(R·C)",
    sc: "O(C)",
    sig: "<b>Signal:</b> paths through a grid, minimum path sum, maximal square, obstacles.",
    why: "dp[r][c] depends on the cell above and to the left. Because you only need the previous row, the 2D table collapses to a 1D rolling array.",
    trap: "When rolling to 1D, the order of the inner loop decides whether <code>dp[c-1]</code> is the current row or the previous one. Get it backwards and you read stale values. When in doubt, keep the 2D table — it is O(R·C) memory and almost always fine.",
    say: "Each cell depends only on the row above, so I can roll the table down to O(C) space.",
    py: "dp = [0] * C\ndp[0] = 1\nfor r in range(R):\n    for c in range(C):\n        if grid[r][c] == 1:      # obstacle\n            dp[c] = 0\n        elif c > 0:\n            dp[c] += dp[c - 1]   # dp[c] = up, dp[c-1] = left\nreturn dp[C - 1]",
    cpp: "vector<long long> dp(C, 0);\ndp[0] = 1;\nfor (int r = 0; r < R; r++)\n    for (int c = 0; c < C; c++) {\n        if (grid[r][c] == 1) dp[c] = 0;\n        else if (c > 0)      dp[c] += dp[c-1];\n    }\nreturn dp[C-1];",
    java: "long[] dp = new long[C];\ndp[0] = 1;\nfor (int r = 0; r < R; r++)\n    for (int c = 0; c < C; c++) {\n        if (grid[r][c] == 1) dp[c] = 0;\n        else if (c > 0)      dp[c] += dp[c-1];\n    }\nreturn dp[C-1];",
    lc: [
      [62, "unique-paths", "Unique Paths"],
      [63, "unique-paths-ii", "Unique Paths II"],
      [64, "minimum-path-sum", "Minimum Path Sum"],
      [221, "maximal-square", "Maximal Square"],
      [931, "minimum-falling-path-sum", "Min Falling Path Sum"],
    ],
  },
  {
    t: "dp",
    n: "Two-sequence DP (LCS / edit distance)",
    tc: "O(n·m)",
    sc: "O(m)",
    sig: "<b>Signal:</b> two strings or arrays compared — longest common subsequence, edit distance, delete operations, interleaving, wildcard matching.",
    why: "dp[i][j] = answer for the first i of A and first j of B. If the characters match, extend the diagonal; otherwise take the best of dropping one from either side.",
    trap: "The table is (n+1)×(m+1) and row/column 0 must be initialised to the empty-string base case. Off-by-one here is the entire difficulty — indices into the strings are <code>i-1</code> and <code>j-1</code>, not <code>i</code> and <code>j</code>.",
    say: "Every two-string problem is the same table; only the recurrence in the match and mismatch branches changes.",
    py: "dp = [[0] * (m + 1) for _ in range(n + 1)]\nfor i in range(1, n + 1):\n    for j in range(1, m + 1):\n        if A[i-1] == B[j-1]:              # note i-1, j-1\n            dp[i][j] = dp[i-1][j-1] + 1\n        else:\n            dp[i][j] = max(dp[i-1][j], dp[i][j-1])\nreturn dp[n][m]\n\n# edit distance mismatch branch:\n#   1 + min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1])",
    cpp: "vector<vector<int>> dp(n + 1, vector<int>(m + 1, 0));\nfor (int i = 1; i <= n; i++)\n    for (int j = 1; j <= m; j++)\n        dp[i][j] = (A[i-1] == B[j-1])\n                 ? dp[i-1][j-1] + 1\n                 : max(dp[i-1][j], dp[i][j-1]);\nreturn dp[n][m];",
    java: "int[][] dp = new int[n + 1][m + 1];\nfor (int i = 1; i <= n; i++)\n    for (int j = 1; j <= m; j++)\n        dp[i][j] = (A.charAt(i-1) == B.charAt(j-1))\n                 ? dp[i-1][j-1] + 1\n                 : Math.max(dp[i-1][j], dp[i][j-1]);\nreturn dp[n][m];",
    lc: [
      [1143, "longest-common-subsequence", "Longest Common Subsequence"],
      [72, "edit-distance", "Edit Distance"],
      [583, "delete-operation-for-two-strings", "Delete Operation"],
      [10, "regular-expression-matching", "Regex Matching"],
      [97, "interleaving-string", "Interleaving String"],
    ],
  },
  {
    t: "bt",
    n: "Subsets & combinations",
    tc: "O(2ⁿ)",
    sc: "O(n)",
    sig: "<b>Signal:</b> enumerate all subsets, combinations, or 'all ways to choose'. n is small — usually ≤ 20.",
    why: "At each index you either include the element or you do not. The recursion tree has 2ⁿ leaves. Passing a start index prevents revisiting earlier elements, which is what makes it combinations rather than permutations.",
    trap: "Append a <b>copy</b> of the path, not the path itself — appending the live list means every result mutates as you backtrack and you end up with n identical empty lists. In Python that is <code>path[:]</code>; in Java <code>new ArrayList&lt;&gt;(path)</code>.",
    say: "The start index is what enforces combinations over permutations, and the tree has 2ⁿ leaves so it's exponential by nature.",
    py: "out = []\ndef backtrack(start, path):\n    out.append(path[:])              # COPY, not path\n    for i in range(start, len(a)):\n        if i > start and a[i] == a[i-1]:\n            continue                 # skip dups (needs sorted a)\n        path.append(a[i])\n        backtrack(i + 1, path)       # i+1: no reuse\n        path.pop()                   # undo\nbacktrack(0, [])\nreturn out",
    cpp: "vector<vector<int>> out;\nvector<int> path;\nfunction<void(int)> backtrack = [&](int start) {\n    out.push_back(path);\n    for (int i = start; i < (int)a.size(); i++) {\n        if (i > start && a[i] == a[i-1]) continue;\n        path.push_back(a[i]);\n        backtrack(i + 1);\n        path.pop_back();\n    }\n};\nbacktrack(0);\nreturn out;",
    java: "List<List<Integer>> out = new ArrayList<>();\nvoid backtrack(int start, List<Integer> path) {\n    out.add(new ArrayList<>(path));      // COPY\n    for (int i = start; i < a.length; i++) {\n        if (i > start && a[i] == a[i-1]) continue;\n        path.add(a[i]);\n        backtrack(i + 1, path);\n        path.remove(path.size() - 1);\n    }\n}",
    lc: [
      [78, "subsets", "Subsets"],
      [90, "subsets-ii", "Subsets II"],
      [77, "combinations", "Combinations"],
      [39, "combination-sum", "Combination Sum"],
      [40, "combination-sum-ii", "Combination Sum II"],
    ],
  },
  {
    t: "bt",
    n: "Permutations",
    tc: "O(n·n!)",
    sc: "O(n)",
    sig: "<b>Signal:</b> all orderings, all arrangements, N-Queens placement, word squares.",
    why: "At each depth try every element not yet used. A used[] array (or swapping in place) tracks availability. Unlike subsets there is no start index, because order matters.",
    trap: "For duplicates, sort first and skip <code>a[i] == a[i-1] and not used[i-1]</code> — the <code>not used[i-1]</code> clause is essential and the most-forgotten condition in all of backtracking. Without it you emit duplicate permutations.",
    say: "No start index because order matters; the used array is what prevents reusing an element within one arrangement.",
    py: "out, used = [], [False] * len(a)\na.sort()                            # needed for dedup\ndef backtrack(path):\n    if len(path) == len(a):\n        out.append(path[:])\n        return\n    for i in range(len(a)):\n        if used[i]:\n            continue\n        if i > 0 and a[i] == a[i-1] and not used[i-1]:\n            continue                # skip duplicate branch\n        used[i] = True\n        path.append(a[i])\n        backtrack(path)\n        path.pop()\n        used[i] = False\nbacktrack([])\nreturn out",
    cpp: "sort(a.begin(), a.end());\nvector<vector<int>> out;\nvector<int> path;\nvector<bool> used(a.size(), false);\nfunction<void()> backtrack = [&]() {\n    if (path.size() == a.size()) { out.push_back(path); return; }\n    for (int i = 0; i < (int)a.size(); i++) {\n        if (used[i]) continue;\n        if (i > 0 && a[i] == a[i-1] && !used[i-1]) continue;\n        used[i] = true;  path.push_back(a[i]);\n        backtrack();\n        path.pop_back(); used[i] = false;\n    }\n};\nbacktrack();",
    java: "Arrays.sort(a);\nList<List<Integer>> out = new ArrayList<>();\nboolean[] used = new boolean[a.length];\nvoid backtrack(List<Integer> path) {\n    if (path.size() == a.length) { out.add(new ArrayList<>(path)); return; }\n    for (int i = 0; i < a.length; i++) {\n        if (used[i]) continue;\n        if (i > 0 && a[i] == a[i-1] && !used[i-1]) continue;\n        used[i] = true;  path.add(a[i]);\n        backtrack(path);\n        path.remove(path.size() - 1); used[i] = false;\n    }\n}",
    lc: [
      [46, "permutations", "Permutations"],
      [47, "permutations-ii", "Permutations II"],
      [51, "n-queens", "N-Queens"],
      [37, "sudoku-solver", "Sudoku Solver"],
      [79, "word-search", "Word Search"],
    ],
  },
  {
    t: "greedy",
    n: "Sort then take greedily",
    tc: "O(n log n)",
    sc: "O(1)",
    sig: "<b>Signal:</b> maximise activities, minimum arrows/platforms/removals, assign to the best fit. The answer is built by repeatedly taking a locally optimal item.",
    why: "Sort by the right key, then commit to each item without reconsidering. For interval scheduling the key is the <b>end</b> time: finishing earliest leaves the most room for everything after.",
    trap: "Sorting by <em>start</em> maximises nothing — it is the classic wrong greedy for activity selection. And greedy needs proof: if you cannot state an exchange argument for why the local choice is safe, it is probably a DP problem in disguise.",
    say: "The exchange argument: swapping in the earliest-finishing compatible interval never makes the solution worse, so the greedy choice is safe.",
    py: "ivs.sort(key=lambda x: x[1])       # by END, not start\ncount, end = 0, float('-inf')\nfor s, e in ivs:\n    if s >= end:                   # compatible\n        count += 1\n        end = e\nreturn count",
    cpp: "sort(ivs.begin(), ivs.end(),\n     [](auto& x, auto& y){ return x[1] < y[1]; });\nint count = 0; long long end = LLONG_MIN;\nfor (auto& iv : ivs)\n    if (iv[0] >= end) { count++; end = iv[1]; }\nreturn count;",
    java: "Arrays.sort(ivs, (x, y) -> Integer.compare(x[1], y[1]));\nint count = 0; long end = Long.MIN_VALUE;\nfor (int[] iv : ivs)\n    if (iv[0] >= end) { count++; end = iv[1]; }\nreturn count;",
    lc: [
      [435, "non-overlapping-intervals", "Non-overlapping Intervals"],
      [452, "minimum-number-of-arrows-to-burst-balloons", "Burst Balloons"],
      [455, "assign-cookies", "Assign Cookies"],
      [621, "task-scheduler", "Task Scheduler"],
    ],
  },
  {
    t: "greedy",
    n: "Reachability sweep",
    tc: "O(n)",
    sc: "O(1)",
    sig: "<b>Signal:</b> jump games, gas stations, can-you-reach-the-end, minimum refuels.",
    why: "Track the furthest position reachable so far. If your current index ever exceeds it, you are stuck. One pass, no DP table — the greedy frontier is all the state you need.",
    trap: "For the <em>minimum jumps</em> variant you need two frontiers (the end of the current level and the furthest seen) — it is really a BFS in disguise. Using the single-frontier reachability code there gives the wrong count.",
    say: "The furthest-reachable frontier is a sufficient summary of everything before i, so one pass suffices.",
    py: "reach = 0\nfor i, jump in enumerate(a):\n    if i > reach:\n        return False          # unreachable\n    reach = max(reach, i + jump)\nreturn True",
    cpp: "int reach = 0;\nfor (int i = 0; i < (int)a.size(); i++) {\n    if (i > reach) return false;\n    reach = max(reach, i + a[i]);\n}\nreturn true;",
    java: "int reach = 0;\nfor (int i = 0; i < a.length; i++) {\n    if (i > reach) return false;\n    reach = Math.max(reach, i + a[i]);\n}\nreturn true;",
    lc: [
      [55, "jump-game", "Jump Game"],
      [45, "jump-game-ii", "Jump Game II"],
      [134, "gas-station", "Gas Station"],
      [871, "minimum-number-of-refueling-stops", "Min Refueling Stops"],
    ],
  },
  {
    t: "bit",
    n: "XOR tricks",
    tc: "O(n)",
    sc: "O(1)",
    sig: "<b>Signal:</b> every element appears twice except one; find the missing number; swap without a temp; single number variants.",
    why: "XOR is its own inverse (x^x = 0) and identity (x^0 = x), and it is commutative. So XOR-ing everything cancels the pairs and leaves the singleton.",
    trap: "For 'every element appears <b>three</b> times except one' XOR alone fails — you need bit-counting mod 3, or the two-mask ones/twos trick. Reaching for plain XOR there is a common wrong answer.",
    say: "XOR is self-inverse, so pairs cancel and the odd one out survives — O(1) space where a hash map would cost O(n).",
    py: "out = 0\nfor v in a:\n    out ^= v\nreturn out\n\n# missing number in 0..n\nout = len(a)\nfor i, v in enumerate(a):\n    out ^= i ^ v\n\n# useful identities\n#   x & (x - 1)   clears the lowest set bit\n#   x & (-x)      isolates the lowest set bit\n#   x & (x - 1) == 0   ->  power of two",
    cpp: "int out = 0;\nfor (int v : a) out ^= v;\nreturn out;\n\n// __builtin_popcount(x)  - number of set bits\n// x & (x - 1)            - clear lowest set bit\n// x & (-x)               - isolate lowest set bit",
    java: "int out = 0;\nfor (int v : a) out ^= v;\nreturn out;\n\n// Integer.bitCount(x)\n// Integer.highestOneBit(x)\n// x & (x - 1)  clears the lowest set bit",
    lc: [
      [136, "single-number", "Single Number"],
      [137, "single-number-ii", "Single Number II"],
      [268, "missing-number", "Missing Number"],
      [191, "number-of-1-bits", "Number of 1 Bits"],
      [338, "counting-bits", "Counting Bits"],
    ],
  },
  {
    t: "bit",
    n: "Bitmask subset enumeration",
    tc: "O(2ⁿ·n)",
    sc: "O(2ⁿ)",
    sig: "<b>Signal:</b> <b>n ≤ 20</b>. That constraint is the interviewer telling you the answer is exponential in n — travelling salesman, assignment, partition into groups.",
    why: "An integer's bits represent a subset. Iterating 0..2ⁿ-1 enumerates every subset, and dp[mask] holds the answer for that subset. Transitions add one element at a time.",
    trap: "Watch operator precedence: <code>1 &lt;&lt; i</code> binds looser than comparison in C/C++/Java, so <code>mask & 1 &lt;&lt; i == 0</code> parses wrongly. Always parenthesise: <code>(mask &amp; (1 &lt;&lt; i)) != 0</code>.",
    say: "n ≤ 20 means 2ⁿ is about a million, so an exponential state space is exactly what the constraints are inviting.",
    py: "# iterate every subset of 0..n-1\nfor mask in range(1 << n):\n    for i in range(n):\n        if mask & (1 << i):        # i is in the subset\n            ...\n\n# iterate all SUBMASKS of mask (classic idiom)\nsub = mask\nwhile sub:\n    ...\n    sub = (sub - 1) & mask\n\nbin(mask).count('1')               # popcount",
    cpp: "for (int mask = 0; mask < (1 << n); mask++)\n    for (int i = 0; i < n; i++)\n        if (mask & (1 << i)) { /* i is in the subset */ }\n\n// all submasks\nfor (int sub = mask; sub; sub = (sub - 1) & mask) { }\n\n__builtin_popcount(mask);",
    java: "for (int mask = 0; mask < (1 << n); mask++)\n    for (int i = 0; i < n; i++)\n        if ((mask & (1 << i)) != 0) { /* parenthesise! */ }\n\nfor (int sub = mask; sub > 0; sub = (sub - 1) & mask) { }\n\nInteger.bitCount(mask);",
    lc: [
      [78, "subsets", "Subsets"],
      [847, "shortest-path-visiting-all-nodes", "Shortest Path Visiting All Nodes"],
      [698, "partition-to-k-equal-sum-subsets", "Partition to k Equal Sums"],
      [1349, "maximum-students-taking-exam", "Maximum Students Taking Exam"],
    ],
  },
  {
    t: "bit",
    n: "Sieve, GCD and fast power",
    tc: "O(n log log n) / O(log n)",
    sc: "O(n)",
    sig: "<b>Signal:</b> primes up to n, counting divisors, modular arithmetic, 'answer modulo 10⁹+7'.",
    why: "The sieve marks composites from each prime's square upward. GCD by Euclid is O(log min(a,b)). Fast exponentiation squares the base while halving the exponent, so xⁿ costs O(log n) multiplications.",
    trap: "Any problem saying <b>modulo 10⁹+7</b> needs the mod applied at every step, and in C++/Java the intermediate product must be <code>long long</code>/<code>long</code> — <code>(a*b) % m</code> with two ints near 10⁹ overflows <em>before</em> the mod is applied.",
    say: "Modular exponentiation is O(log n) by repeated squaring, and I'll cast to 64-bit before multiplying to avoid overflow.",
    py: "# sieve\nis_prime = [True] * (n + 1)\nis_prime[0] = is_prime[1] = False\nfor p in range(2, int(n ** 0.5) + 1):\n    if is_prime[p]:\n        for m in range(p * p, n + 1, p):   # start at p*p\n            is_prime[m] = False\n\nfrom math import gcd\nlcm = a * b // gcd(a, b)\n\npow(base, exp, MOD)        # built-in modular exponentiation",
    cpp: "vector<bool> isPrime(n + 1, true);\nisPrime[0] = isPrime[1] = false;\nfor (long long p = 2; p * p <= n; p++)\n    if (isPrime[p])\n        for (long long m = p * p; m <= n; m += p) isPrime[m] = false;\n\nlong long power(long long b, long long e, long long mod) {\n    long long r = 1; b %= mod;\n    while (e) {\n        if (e & 1) r = r * b % mod;   // long long BEFORE the mod\n        b = b * b % mod;\n        e >>= 1;\n    }\n    return r;\n}",
    java: "boolean[] isPrime = new boolean[n + 1];\nArrays.fill(isPrime, true);\nisPrime[0] = isPrime[1] = false;\nfor (long p = 2; p * p <= n; p++)\n    if (isPrime[(int) p])\n        for (long m = p * p; m <= n; m += p) isPrime[(int) m] = false;\n\nlong power(long b, long e, long mod) {\n    long r = 1; b %= mod;\n    while (e > 0) {\n        if ((e & 1) == 1) r = r * b % mod;\n        b = b * b % mod;\n        e >>= 1;\n    }\n    return r;\n}",
    lc: [
      [204, "count-primes", "Count Primes"],
      [50, "powx-n", "Pow(x, n)"],
      [372, "super-pow", "Super Pow"],
      [1250, "check-if-it-is-a-good-array", "Good Array (gcd)"],
    ],
  },
] as const;
